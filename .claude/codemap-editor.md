<!-- stamp: a082d5f 2026-08-21 status: DEGRADED 2 stale, 0 orphan, 0 missing-role -->
Assets/Editor/SceneBootstrapper.cs | builds the three scenes | sys: Tooling | api: BootstrapScenes() | dep: SceneNames,GameRoot | used: - | crit: K2 | note: Tools > Colorful Sort > Bootstrap Scenes; idempotent, so it never overwrites a furnished scene; also sets Build Settings and deletes the TemplateLeftovers assets Also seeds Data/Config/DisplayConfig.asset when it is missing and wires it into Boot GameRoot when that reference is empty - seeded, never overwritten, the same line BlockSkinFactory draws between creating an asset and deciding its content (D-020). SeedTargetFrameRate is a starting value a designer overwrites in the Inspector, not the authority; the runtime reads only the asset (D-100). | h:101da77c
Assets/Editor/ArtImportPass.cs | puts the pack contract on sprites | sys: Tooling | api: ApplyImportSettings() | dep: - | used: - | crit: K2 | note: Tools > Colorful Sort > Apply Art Import Settings; PPU 512, slot-family pivots and the two Tiled borders (D-007), all given by the pack's import guide; idempotent, reimports only what differs; the UI sprites joined in task 4A on the same terms: their 9-slice borders come from the pack's AssetManifest.json and they keep its 100 pixels per unit, because a Canvas scales by its reference resolution and never reads pixels-per-unit Carries the hand-drawn column art at its own 160 px per unit — a measured number, since the tray's drawn interior is 188 px and no nine-slice border can state where a painted rail ends — and states every column border as skirt/crown only (D-048), the ice and old normal sprites re-stated to the same skirts they already had. The tray's top border is 28 px because that number does two jobs: it is the largest distance at which the drawn interior begins across the sprite's width, so the whole wave stays out of the stretched band, and it is where the crown puts the top cell's ceiling — leaving the highest brick's studs inside the wave instead of under it (D-051). block_glow shares the completed-shadow contract exactly, because it is that drawing with its pixels whitened so a tint can light it rather than darken it (D-060). block_glow shares the *tray's* contract, since it is the tray's drawing whitened: same pivot, same 160 px per unit, same border — which is what lets the view read a tray's metrics off it (D-062). block_glow now has its *own* contract: the tray's scale and pivot but a 42 px top border rather than 28, because the drawing's corner only settles at y 40 and a nine-slice stretches whatever the border leaves out — that stretch is what squared the glow's corner (D-068). Nothing reads that sprite's metrics, so the two borders may differ. plume_puff joins on its own terms: it is read as a *texture* by a particle material rather than as a sprite by a renderer, so its pivot and scale are never used and only its alpha matters (D-074). It also states npotScale explicitly: a texture that arrived as Default carries ToNearest, which a Sprite may not have, so writing the settings back made Unity reset it and warn once per sprite (D-069). | h:de0eaa80
Assets/Editor/BlockSkinFactory.cs | creates block skins and materials | sys: Tooling | api: CreateBlockSkins() | dep: BlockSkin,BlockSkinSet | used: - | crit: K2 | note: Tools > Colorful Sort > Create Block Skins; finds Block_*.fbx, wires the mesh inside each one and generates a URP Lit material from the skin's authored colour (D-020); never overwrites an assigned mesh, material or id mapping, because those assignments are the re-skin (D-003) Writes the symbol materials in a second pass, after the set exists, because their colour is the skin's own times the set's shade (D-052); skips Block_Base, which shares the models folder and the prefix but is a floor plate, not a colour. WireSparkMesh extracts each skin's symbol into Art/Models/Blocks/Symbols/ and, unlike WireMesh beside it, runs on EVERY pass - the brick mesh is a reference to authored art, this is DERIVED from it, so it is stale the moment the FBX changes and 'skip what exists' would leave the old symbol in place with nothing to say so. It copies into the asset already at the path rather than replacing the file, so the GUID and every reference to it survive the rebuild (D-080). The symbol pass asks the set for each symbol colour rather than doing the arithmetic itself, so the `?` getting an authored colour instead of a derived one needed no change here (D-099). | h:bbc91d5d
Assets/Editor/UnityMapExporter.cs | editor-side unitymap and assetmap exporter | sys: Tooling | api: Export(); ExportBoth(); AssetMap() | dep: - | used: - | crit: K3 | note: unity-dev tooling, byte-identical copy of .claude/templates/Editor/; real type information, which is what clears the python fallback's missing-script findings | h:4547f614
Assets/Editor/BoardViewPrefabFactory.cs | builds board prefabs and wires the scene | sys: Tooling | api: BuildPrefabs(); WireGameScene() | dep: ColumnView,BlockView,BoardView,BoardInput,BoardMoveAnimator,BoardLayoutConfig,BoardAnimationConfig,BlockSkinSet,LevelDatabase,ProgressionConfig,AttemptStarter | used: - | crit: K2 | note: Tools > Colorful Sort > Build BoardView Prefabs / Wire Game Scene; encodes the pack's sorting orders, derives the layering stand-off from the measured brick depth (D-028) and gives the ice variant its thawed sprite (D-030); non-destructive — existing prefabs untouched, only empty scene slots filled; since 5A it fills AttemptStarter's database and progression config instead of a fixture level, because which level opens is progression's answer now Now repairs as well as creates: RepairColumnPrefabs brings an existing column prefab up to the current anatomy part by part — the hand-drawn tray in place of the pack's, the divider sprite, and the Block_Base plate placed by measurement (its top face lands on the first cell's floor, which is what the tray's 46 px skirt was chosen to match). Prefab variants are skipped on purpose: a child added to the base reaches them, and loading a variant's contents to save it back is how a variant loses its parent. The base plate is placed by a relationship rather than an offset — its studs end where the studs of a brick standing in the cell below would end — and the position is written on every run, not only when the plate is created (D-051); BrickMeshes excludes Block_Base, which shares the folder and would otherwise be measured as a brick. Generates Slot_Base.mat for the plate with its base colour sampled from the tray PNG on disk — the FBX's own material arrives on a shader URP does not know and draws magenta — and re-materials an existing plate as well as re-seating it (D-052). An existing plate's transform is left alone again (D-053): the derived seating is what a new plate is created with, and a value a designer nudged by eye beats a formula — rewriting it every run was this tool quietly undoing their tuning. Only a missing or FBX-imported material is replaced. Adds the settled shadow to a column prefab (transparent, at the cover's own stand-off so it lands in front of the bricks) and wires the view's animation config, both idempotently (D-057). Also creates the board's single Glow renderer under Columns, sorted between the tray and the bricks, and wires it (D-060). Creates Block_Glow.mat from the project's additive glow shader once and leaves it alone, since alpha blending cannot glow (D-061). The glow's material is ensured on every run, not only when the renderer is created — the create-only version worked exactly once, which is the same mistake the base plate taught in V-3b; a material that is not Unity's sprite default is somebody's choice and is left alone (D-061). EnsureBlockPlume does the same for the brick's plume — the child, its ParticleSystem, Block_Plume.mat from the same additive shader with the puff as its texture, and the reference in BlockView, all ensured on a prefab that already exists, because BuildBlockPrefab returns the moment it does. It also deletes the Trail child and Block_Trail.mat an earlier version of itself made, since a line was the wrong reading of the reference and both would draw. World simulation space, emission by distance and the noise are what make it a plume rather than a row of beads; they and the fade curves are written once at creation and never again (D-053, D-074). EnsureColumnSparkle joins the column repair chain with the same shape: one particle system per column, emitting nothing by itself — the view says when, how many and from where — sharing the plume's additive material until the sparks take the symbol's own outline (D-075). The column's single Sparkle is retired by name and replaced by two children - Finish (gravity) and Rise (none) - which is what keeps both create-only and makes the migration run once, the same shape the plume used to retire the trail (D-074). Beside the create-only dressing there is now a repair pass that runs every build and writes only what must be TRUE of a particle system rather than what somebody tuned: play-on-awake, world simulation space, max particles, cleared emission rates, no gravity on the burst whose authored climb depends on having none, and all three velocity axes in one curve mode. That last one is the console's 'Particle Velocity curves must all be in the same mode': DressPlume wrote velocity.y as a two-constant range and left x and z single constants, and a dressing pass that only runs at creation could never reach the prefab already carrying it - the seventh time this project met the create-only trap, and the second time it was designed around instead of discovered (D-078). The bursts stopped sharing the plume's material: that is the GLOW's shader, whose depth test is what leaves only a rim showing behind a lifted run, and a spark emitted inside a brick's own mesh was therefore hidden exactly as designed. Block_Spark.mat is the same drawing and shader with _ZTest Always - a second material rather than a second shader, since the difference is one piece of render state. The pass replaces the plume material where it finds it rather than filling only an empty slot, because a pass that fills only empties cannot correct a choice it made itself last task (D-071, D-079). Block_Symbol.mat joins the two: the same shader at the same Z test with NO texture, because once a particle is a mesh the shape is its silhouette and a puff sampled over a symbol's own UVs is noise. Block_Spark.mat is kept rather than deleted - pointing the renderers back at it is the whole of the revert. The material pass now replaces anything it has ever assigned itself, which is the same lesson for the eighth time (D-071, D-080). RepairBurst stops clearing the placement burst's gravity every build. D-078 put that clause there when the rise was one flat speed and any pull made the authored height a lie; the rise decelerates now and the view computes the pull per burst, so the clause would be fighting the value's own authority. What stays repaired is what nobody tunes and nothing computes (D-081). DressBurst is a starting point now rather than a look: the shape, the noise and the motion belong to the view, and what stays the prefab's is the renderer and the fade curves (D-082). The wander frequency is read off ColumnView rather than copied, the way the base plate's name already is (D-071). | h:78caba2d
Assets/Editor/LevelBoardDrawer.cs | draws a level board grid | sys: Tooling | api: BoardHit(int,int,int); BoardHit.None; GridCell; Column; Cell; HitGrid; HitColumn; HitCell; ColumnSnapshot(ColumnKind,int,IReadOnlyList<CellDefinition>,int,int); ColumnSnapshot.BlockCount; ColumnSnapshot.FromColumnData(ColumnData); CellWidth; CellHeight; HeaderHeight; ColumnGap; RowGap; Size(IReadOnlyList<ColumnSnapshot>,int,int); Draw(Rect,IReadOnlyList<ColumnSnapshot>,IReadOnlyList<int>,int,int,BlockSkinSet,int,bool,bool); SwatchOf(BlockSkinSet,int); SymbolOf(BlockSkinSet,int) | dep: BoardLayout,BlockSkinSet,BlockSkin,BlockColourId,ColumnKind,ColumnData,CellDefinition | used: LevelEditorWindow | crit: K3 | note: draws every cell of the layout grid, empty ones included, because an empty cell is where a column can be placed (D-033); centreRows switches between the literal grid you author on and the centred rows the game draws (D-034); holds no colour of its own, every swatch is read from the single BlockSkinSet (D-003, D-020) | h:a0f052e2
STALE Assets/Editor/LevelEditorWindow.cs | authoring window for level assets | sys: Tooling | api: Open() | dep: LevelDatabase,LevelBoardDrawer,BoardHit,ColumnSnapshot,BoardLayout,BoardSolver,SolverResult,SolverVerdict,SolverMove,LevelDefinition,ColumnDefinition,CellDefinition,BlockSkinSet,BlockSkin,BlockColourId,ColumnKind,ColumnData,LevelData,AttemptScramble | used: - | crit: K3 | note: Tools > Colorful Sort > Level Editor (D-010); two gates side by side — Validate() runs every repaint and the solvability search runs on demand, its verdict dropped the moment the board changes because a verdict is true of one exact board; columns are placed by clicking an empty grid cell and moved with an armed "Move to cell" that swaps with whoever is there, and the placement array is kept parallel to the columns on every structural edit (D-033); writes through SerializedObject/SerializedProperty instead of new setters on Content and resolves every path once per bound level (D-032); offers shapes but never re-states ColumnData's rules — LevelDefinition.Validate() is the shippability verdict and the variant stepper shows the looks a player can meet (D-014, D-015); since 5A it also rebuilds LevelDatabase from the level folder in plaque order (Rebuild database) — the database is level data and this is where level data is authored, and it is written through SerializedProperty like everything else here (D-032) It edits the FILE now, not a level asset: a popup over the levels it decoded, Save encoding all of them back through LevelCodec, and Reload for a file changed underneath it. RebuildDatabase is gone - there are no assets to collect, so what is left is EnsureDatabase pointing the database at the file, still through SerializedProperty (D-032). New Level builds a level by DECODING AN EMPTY ROW rather than reaching for a constructor, which keeps the codec the only way a level comes into existence and is why Content needed no public setter for the editor's sake. The levels it holds are transient and it destroys them - nothing else will, since they are HideAndDontSave - and it remembers the open level by its plaque number rather than by reference, because a transient object does not survive a domain reload (D-085). New Level starts at LevelDefinition.FirstLevelIndex rather than 0 (D-086). A blank level starts on a 2x6 grid, the reference's own shape. It was 1x1, and since a column is added by CLICKING a grid cell that offered exactly one cell and no second column could ever be placed - a dead end that looked like a working level (D-087). Saving now also names every level in the file the board would refuse, not just the open one — three shipped hiding cells in Normal columns and the first anybody heard of it was the level refusing to open in play. Saving is still not blocked on it, because half-built work has to be storable; the test over the shipped file stays the guard (D-097). The brush itself can no longer author the state that report names: painting a hidden cell into a Normal column promotes it to Mystery, which is the only kind that can hold one — RevealsTopWhenExposed is Mystery alone, Ice holds no authored cells and Covered hides every cell behind a key. D-097's save-time report did not stop the recurrence because it arrives after the mistake; four more levels reached the file and a phone build the next day, so the refusal moved to the paint site (D-102). | h:b294d974
STALE Assets/Editor/UiFactory.cs | builds the ui foundation and wires it | sys: Tooling | api: BuildUi() | dep: UiStyleConfig,PopupHost,Popup,PausePopup,WinPopup,FailPopup,BoosterShopPopup,SettingToggleButton,BoosterButton,BoosterId,CoinHud,CoinFlight,SafeAreaPanel,GameplayHud,MainMenu,AttemptStarter | used: - | crit: K2 | note: Tools > Colorful Sort > Build UI; generates the one TMP material from UiStyleConfig the way BlockSkinFactory generates a brick's material from its colour (D-020), builds the three popups and the booster button, gives Boot the persistent popup canvas plus the project's single EventSystem, builds the HUD in Game and the Play button in Menu — all three scenes, because Boot's host and EventSystem are unreachable until something loads Game over Boot. Three things it sets are invisible when wrong: the scaler's 1080x1920 / 0.5, the InputSystemUIInputModule (this project has no legacy input backend, so the standalone module is compiled out and would leave every button silently dead), and which graphics are raycast targets — a HUD panel left as one swallows every tap meant for the board. Non-destructive: an existing prefab, material or asset is the authority and is never overwritten, and only empty scene slots are filled — so the layout is the user's to hand-edit and re-running never undoes it. Both the HUD's build and its wiring are per-part and idempotent: each of the plaque, the gear and the booster bar is added only if absent, and the wiring pass runs whether the HUD was just made or was already there, re-loading the style config from disk. That shape was earned twice — an existing-but-half-wired HUD was once unreachable because the method returned the moment it saw one, and then an all-or-nothing build left the booster bar unbuilt for exactly the same reason. A tool that cannot repair what an older version of itself made only works once. It also reopens Boot when it finishes: each pass opens its scene in Single mode, so it used to leave a screen scene loaded, and pressing Play there runs it without GameRoot, the popup host or the EventSystem — a dead Play button with nothing in the console. NewPopupRoot was extracted at the third caller, not the first — Pause keeps the shell it built before there was anything to share it with. Pause's row holds the three toggles and only those: Restart moved out to a wide button, having been the one round button whose off-state graphic was built and thrown away, since a command has no on/off state (D-045). Toggle icons come from the second UI set through LoadFmSprite — the first pack ships no crossed-out variant and no music icon — and are named from one stem so a pair cannot be given mismatched halves by a typo The booster button's icon is ensured on every run and each instance is dressed with its own sprite — stack, undo, shuffle — outside the branch that builds the bar, because a bar that already existed is exactly the case a create-only pass cannot reach (D-068); an icon somebody already chose is left alone. Ensuring the object was not enough: it was added carrying no sprite and an Image with no sprite draws nothing, so the prefab now gets a default icon and the dressing pass may replace a sprite from its own list — filling only an empty slot cannot survive a sensible default (D-071). The menu's three new references - its label, the level database and the style config - are filled here like every other one on that screen; the button's authored text is only what shows in Prefab Mode before MainMenu writes the real one (D-086). Task 5B-4 adds four builds and three repairs, all on the same terms: Popup_BoosterShop (one prefab for three boosters, its title and blurb written at runtime), CoinFlyer (one Image, never a raycast target, so a coin crossing the win popup cannot swallow the press on its button), the coin pill in the Game HUD and the coin flight layer under Boot's --UI-- with overrideSorting at 300 - a nested canvas, so there is no second scaler to keep in step with the 1080x1920 contract, and no GraphicRaycaster. The repairs are the create-only lesson for the fifth and sixth time (D-068, D-071): EnsureBoosterBadges hangs the count disc and the plus on a prefab four tasks older than either, and EnsureWinReward gives the win popup its coin anchor. BuildBoosterButton stopped being a branch that returns - it creates if absent and then always dresses - which is the shape the branch itself kept defeating. SpawnBooster writes the booster with intValue rather than enumValueIndex, the rule SettingToggleButton already states. 5B-5 turns three of those builds around (D-092): the coin flight layer and CoinFlyer.prefab are RETIRED - the layer by NAME, which is the only way to find a GameObject whose script is already deleted - and the win popup's RewardAnchor goes with them, replaced by a coin and an amount the popup writes at runtime. The coin pill is MOVED rather than rebuilt: the object in the Game HUD is the one the user tuned, so it is copied whole into Popup_BoosterShop and deleted from the scene, and only a project with nothing to move gets a fresh one built. Mirror() copies the count badge's rect onto the plus badge on every run, in the prefab and on all three instances - an override the prefab cannot see is the version the player looks at - which makes the count the badge you tune and the plus the one that follows. EnsureBoosterShopScrim adds the shop's OWN darkening layer as its first child, on top of the host's - the two stack on purpose and the shop sits behind about 0.8 rather than 0.55 (D-093); added only when absent, so its alpha is the user's from then on. EnsureBoosterShopWiring fills whichever of the shop popup's slots are EMPTY, by name and at any depth - written after deleting the popup's header by hand left titleLabel pointing at nothing, so every booster showed the prefab's authored placeholder instead of its own name. The component's null guard was right; what was missing was anything able to notice an empty slot once the build pass had returned, which is D-068/D-071 at the ninth time of asking. FindDeep beside Find is the whole difference: a lookup that insisted on Body/Header/Title would fail for exactly the edit that caused the bug. | h:3f42f40d
Assets/Editor/UiPrefabEnvironmentSetup.cs | makes ui prefabs editable in prefab mode | sys: Tooling | api: SetUp() | dep: UiFactory | used: - | crit: K3 | note: Tools > Colorful Sort > Set Up UI Prefab Editing; builds Assets/Editor/PrefabEnvironments/UI.unity — one canvas, built by calling UiFactory.EnsureCanvas rather than retyping the 1080x1920 and match 0.5 that every screen scales by — and assigns it to EditorSettings.prefabUIEnvironment so double-clicking a popup prefab opens it at real proportions. The popups exist only as prefabs because PopupHost instantiates them, which is why Prefab Mode had no canvas to show them in. The scene is under Assets/Editor/ so it is in no build, and is deliberately absent from the blueprint's scene inventory, which describes what the game loads; it reopens Boot when it finishes, for the same reason UiFactory does | h:132d4d1c

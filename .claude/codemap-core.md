<!-- stamp: 3c415f0 2026-08-21 status: OK -->
Assets/Scripts/Core/ColorfulSort.Core.asmdef | core assembly with no references | sys: Core | api: asmdef ColorfulSort.Core | dep: - | used: ColorfulSort.Core.Tests | crit: K1 | note: references [] makes the blueprint's "Core depends on: -" compiler-enforced (D-016); autoReferenced so UI/Meta/Content still see it | h:6d910c56
Assets/Scripts/Core/SaveData.cs | the save file as plain fields | sys: Core | api: CurrentVersion; saveVersion; playerId; currentLevelOrdinal; coins; hearts; heartRefillUnixMs; soundOn; musicOn; vibrationOn; levels; boosters; NewGame(); NewPlayerId(); LevelProgressRecord.levelOrdinal,cleared,plays; BoosterChargeRecord.boosterId,charges | dep: - | used: SaveService,SaveMigration,GameRoot | crit: K1 | note: public fields because JsonUtility reads nothing else; a fresh save carries no economy defaults — hearts and charges are Data/Config's and Meta's to seed; CurrentVersion is 2 and musicOn arrived with it — a bool JsonUtility defaults to false cannot say whether the player chose off or never had the field, so the version step answers it rather than the field (D-045) | h:c494b97f
Assets/Scripts/Core/SaveMigration.cs | load-time save version gate | sys: Core | api: TryMigrate(SaveData,out SaveData,out bool,out string) | dep: SaveData | used: SaveService | crit: K1 | note: refuses an unversioned or newer-than-this-build file instead of guessing (D-019); upgrade steps go in the while loop, one per version — 1 -> 2 turns musicOn on for a file written before the field existed (D-045); Repair fixes structure only, never Meta's numbers; the step's `changed` is OR-ed with Repair's, not overwritten by it, or an upgraded save with nothing left to repair would never be flushed and the file would stay a version behind its own memory | h:56592a52
Assets/Scripts/Core/SaveService.cs | the save file's only writer | sys: Core | api: FileName; SaveService(string); FilePath; Data; IsDirty; Load(); MarkDirty(); Flush(); SaveLoadOutcome.Created,Loaded,Recovered | dep: SaveData,SaveMigration | used: GameRoot | crit: K1 | note: directory injected so EditMode tests drive the real file; atomic temp-then-replace; writes only at flush points (D-018); an unreadable save is kept aside, never rewritten | h:560cf6b9
Assets/Scripts/Core/SceneNames.cs | the three scene names, once | sys: Core | api: Boot; Menu; Game; BuildOrder | dep: - | used: SceneFlowService,SceneBootstrapper | crit: K2 | note: the bootstrapper fills Build Settings from BuildOrder, so the build list and the names the flow asks for cannot drift apart | h:daa76478
Assets/Scripts/Core/SceneFlowService.cs | additive screen scene swapping | sys: Core | api: ScreenChanged; ActiveScreen; IsBusy; ShowMenu(); ShowGame() | dep: SceneNames | used: GameRoot | crit: K1 | note: one screen over Boot at a time and Boot is never unloaded; a request arriving mid-swap is refused with a warning, not queued | h:3f5e3905
Assets/Scripts/Core/AttemptSeedSource.cs | the only legal attempt seed | sys: Core | api: For(int,int) | dep: - | used: - | crit: K1 | note: pure f(level ordinal, attempt ordinal) — no clock and no UnityEngine.Random (D-017); Meta passes the save's plays counter; own mixer because Core cannot see Board | h:ff22b52f
Assets/Scripts/Core/GameRoot.cs | boot composition root | sys: Core | api: Instance; Save; Scenes; SoundOn; MusicOn; VibrationOn | dep: SaveService,SceneFlowService,SaveData,SceneNames | used: SceneBootstrapper | crit: K1 | note: on Boot's --Systems-- root because DontDestroyOnLoad only keeps roots; flushes on pause, focus loss and quit; the single writer of all three settings toggles — music is its own flag, not a second meaning for sound, because Pause offers them as two buttons and a muted soundtrack keeps the taps audible Holds the DisplayConfig and applies its frame-rate ceiling in Awake, before the save loads. A missing config is said out loud rather than defaulted, because leaving targetFrameRate alone is a CHOICE of the platform rate - 30 fps on mobile - not a neutral fallback (D-100). | h:16e323a5
Assets/Scripts/Core/DisplayConfig.cs | what the app asks of the display | sys: Core | api: PlatformDefault; TargetFrameRate; CapsFrameRate; Validate(out string) | dep: - | used: GameRoot,SceneBootstrapper | crit: K3 | note: one number so far, the frame-rate CEILING - a 60 Hz screen still gives 60 whatever it says. It is an asset and not a const at the boot site because a frame cap is the most ordinary performance dial there is and rules/data.md is explicit about tuning living in data; the const was one line and was refused because the no-numbers-in-code invariant holds in every phase (D-100). Unity offers no project setting for this, so somewhere HAS to say the number and this is that somewhere. 0 is legal and means "leave the platform alone" - which is not neutral, it is 30 fps on mobile, and the tests pin exactly that; a NEGATIVE rate is refused because it is neither a cap nor a hand-off. | h:714795ed

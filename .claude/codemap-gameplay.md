<!-- stamp: 3c415f0 2026-08-21 status: OK -->
Assets/Scripts/Gameplay/Board/BlockColourId.cs | logical block colour id | sys: Board | api: MinId; MaxId; None; Value; IsNone; Equals(BlockColourId); ==; != | dep: - | used: LevelData,BoardColumn,BoardState,BoardRules,BoardMove,ColumnModifiers,BoardSession | crit: K1 | note: id only; the colour and symbol it maps to live in Content's BlockSkinSet (D-003) | h:86f5193c
Assets/Scripts/Gameplay/Board/ColumnKind.cs | four column kinds enum | sys: Board | api: Normal; Ice; Covered; Mystery | dep: - | used: LevelData,BoardColumn,ColumnModifiers,ColumnDefinition | crit: K1 | note: closed enum on purpose — no IColumnModifier until a real fourth kind exists | h:85d6db73
Assets/Scripts/Gameplay/Board/LevelData.cs | plain-C# level input contract | sys: Board | api: CellData(BlockColourId,bool); ColumnData(ColumnKind,int,IReadOnlyList<CellData>,int,BlockColourId); ColumnData.BlockCount; LevelData(int,IReadOnlyList<ColumnData>); LevelData.Columns; LevelData.BlockCount | dep: BlockColourId,ColumnKind | used: BoardState,BoardSession,LevelDefinition | crit: K1 | note: validates in the ctor, so an illegal level cannot exist; carries no presentation data | h:0c10cb28
Assets/Scripts/Gameplay/Board/DeterministicRandom.cs | rewindable seeded attempt rng | sys: Board | api: DeterministicRandom(int,int); Seed; Cursor; NextInt(int); Rewind(int) | dep: - | used: BoardSession | crit: K1 | note: counter-based so undo restores it with one int (D-002); splitmix64 mix | h:603bcf7b
Assets/Scripts/Gameplay/Board/BoardColumn.cs | live column cells and lock state | sys: Board | api: Kind; Capacity; ThawAfterCompletions; CoverKeyColour; IsLocked; Count; IsEmpty; IsFull; FreeCells; TopColour; IsTopHidden; ColourAt(int); IsHiddenAt(int) | dep: LevelData,ColumnKind,BlockColourId,ColumnModifiers | used: BoardState,BoardRules,ColumnModifiers,BoardSession | crit: K1 | note: every mutator is internal — the view physically cannot write board state; SetColourAt joined them for the shuffle, which rearranges colours in place rather than moving blocks, and it leaves the hidden flag alone because a shuffle never touches a hidden cell (D-011) | h:578118ad
Assets/Scripts/Gameplay/Board/BoardState.cs | columns plus completed-colour set | sys: Board | api: FromLevel(LevelData); LevelIndex; Columns; ColumnCount; this[int]; CompletedColourCount; HasEverCompleted(BlockColourId) | dep: BoardColumn,LevelData,BoardRules,BlockColourId | used: BoardRules,BoardSession | crit: K1 | note: refuses a level that starts with a colour already gathered; win/deadlock are not stored here. The column array stopped being readonly for the add-column booster — AppendColumn/RemoveLastColumn are the only way a column appears or leaves after FromLevel, they hold to LevelData.MaxColumns rather than restating the 16, and removing a column that still holds blocks throws instead of deleting them | h:aa32a926
Assets/Scripts/Gameplay/Board/BoardRules.cs | pure move win deadlock rules | sys: Board | api: TopRunLength(BoardColumn); CanLift(BoardState,int); MovableCount(BoardState,int,int); CanMove(BoardState,int,int); IsColumnSolved(BoardState,int); IsColourComplete(BoardState,BlockColourId); IsWin(BoardState); HasAnyLegalMove(BoardState); IsDeadlock(BoardState) | dep: BoardState,BoardColumn,ColumnModifiers,BlockColourId | used: BoardState,BoardSession | crit: K1 | note: no caching, no flags — recomputed per query, 256 pair tests at event frequency HoldsCompletedColour locks the column of a finished colour — no lift, no move from it (D-056) — and because CanLift and MovableCount are the one gate, the tap, the deadlock test and the solver all see the same narrower move set; it is board-wide completeness, not IsColumnSolved's local single-colour question, and it is computed rather than cached. | h:aae31765
Assets/Scripts/Gameplay/Board/BoardMove.cs | recorded mutation for exact undo | sys: Board | api: MoveKind.Run,AddColumn,Shuffle; ShuffledCell(CellRef,BlockColourId); ShuffledCell.Cell,PreviousColour; BoardMove.NoColumn; Kind; FromColumn; ToColumn; Count; Colour; RngCursorBefore; RevealedCells; ThawedColumns; UncoveredColumns; CompletedColours; ShuffledCells | dep: BlockColourId | used: BoardMoveHistory,BoardSession | crit: K1 | note: the undo contract; the discriminator its earlier note predicted arrived with the boosters, and the run fields sit at NoColumn when a move is not a run. A shuffle records the colour every cell held BEFORE it ran rather than leaving undo to rewind the RNG and recompute the permutation (D-041) — the derived version was this task's preflight assumption and is exactly the failure the same preflight named worst, an undo off by one draw restoring a board that looks right | h:04210af4
Assets/Scripts/Gameplay/Board/BoardMoveHistory.cs | capped move list behind undo | sys: Board | api: MaxEntries; Count; CanUndo; DroppedEntryCount; Entries; Last | dep: BoardMove | used: BoardSession | crit: K1 | note: 256-entry hard bound from fingerprint.md; drops the oldest and reports it | h:23feeedc
Assets/Scripts/Gameplay/Board/ColumnModifiers.cs | ice cover mystery pure rules | sys: Board | api: StartsLocked(ColumnKind); IsPlayable(BoardColumn); ShouldThaw(BoardColumn,int); ShouldUncover(BoardColumn,BlockColourId); RevealsTopWhenExposed(ColumnKind) | dep: BoardColumn,ColumnKind,BlockColourId | used: BoardColumn,BoardRules,BoardSession | crit: K2 | note: one trigger, two readers (D-009) — ice counts completions, a cover matches its key | h:6d51d303
Assets/Scripts/Gameplay/Board/BoardSession.cs | one attempt command surface | sys: Board | api: BoardSession(LevelData,int,AttemptScramble); Level; AuthoredLevel; Scramble; State; Random; History; Seed; IsWon; IsDeadlocked; CanUndo; CanLift(int); CanMove(int,int); MovableCount(int,int); TryMove(int,int); TryMove(int,int,out BoardMove); CanAddColumn; TryAddColumn(); TryAddColumn(out BoardMove); CanShuffle; TryShuffle(); TryShuffle(out BoardMove); Undo(); MoveApplied; MoveUndone; ColourCompleted; CellRevealed; ColumnUnlocked; Won; Deadlocked | dep: BoardState,BoardRules,BoardMove,BoardMoveHistory,ColumnModifiers,DeterministicRandom,LevelData,AttemptScramble | used: - | crit: K1 | note: the only writer of board state; the scramble is a required ctor argument. The two boosters that change the rules live here beside TryMove and go through the same recorded-move path: an added column is empty, Normal, and takes the largest capacity on the board rather than a number chosen in code; a shuffle rearranges the readable cells of unlocked columns among themselves with Fisher-Yates in exactly n-1 draws, keeps every column's fill count, and lets a completed colour complete with every consequence because a shuffle is a move like any other. Both refuse rather than record an empty entry, since one wasted entry costs one of the 256 the Undo booster depends on | h:2020c8bf
Assets/Scripts/Gameplay/Board/AttemptScramble.cs | one of a level's enumerable looks | sys: Board | api: None; NoVariant; VariantIndex; IsIdentity; ForVariant(LevelData,int); ColourOf(BlockColourId); AuthoredColumnAt(int); Apply(LevelData) | dep: LevelData,BlockColourId,DeterministicRandom | used: BoardSession | crit: K2 | note: pure function of (level index, variant index) (D-015); solvability-preserving isomorphism (D-014); knows neither the variant count nor which one to play — both are Meta's, from Data/Config/ | h:fa93282f
Assets/Scripts/Gameplay/Board/ColorfulSort.Board.asmdef | engine-free board assembly definition | sys: Board | api: asmdef ColorfulSort.Board | dep: - | used: ColorfulSort.Board.Tests | crit: K1 | note: noEngineReferences true, so the compiler enforces the no-UnityEngine invariant (D-001); autoReferenced so Content and the view can see it | h:8726fcee
Assets/Scripts/Gameplay/BoardView/ColorfulSort.BoardView.asmdef | scene-side board assembly | sys: BoardView | api: asmdef ColorfulSort.BoardView | dep: ColorfulSort.Board,ColorfulSort.Content,Unity.InputSystem,UnityEngine.UI | used: ColorfulSort.BoardView.Tests | crit: K1 | note: the blueprint's arrow made compiler-enforced (D-024); an asmdef cannot see Assembly-CSharp, which is also what lets the layout maths be unit-tested; Unity.InputSystem was added for part 2 because this project runs the new input backend only (activeInputHandler: 1) and BoardInput reads Pointer.current; UnityEngine.UI joined in task 4A for EventSystem, which is the engine's input router and not this project's UI system (D-037) | h:73c41921
Assets/Scripts/Gameplay/BoardView/BoardLayout.cs | pure column grid, framing, placement, hit test | sys: BoardView | api: ColumnMetrics(float,float,float,int); ColumnMetrics.FromSprite(Sprite); Width; Skirt; Crown; CellsInSprite; Height(int); CellCentreY(int); BoardLayout.NoSlot; SlotAt(Vector2,IReadOnlyList<int>,int,float,float,float,float); CellToGrid(int,int,out int,out int); GridToCell(int,int,int); BoardSize(IReadOnlyList<int>,int,float,float,float,float); SlotBottomCentre(int,IReadOnlyList<int>,int,float,float,float,float); SlotInCell(IReadOnlyList<int>,int); PlaceColumns(IReadOnlyList<int>,int,int,int,int,out int); OrthographicSize(Vector2,float,float); OrthographicSize(Vector2,float,float,float,float); CameraCentreOffset(float,float,float) | dep: - | used: BoardView,ColumnView,LevelBoardDrawer | crit: K1 | note: a column's size is read off its own sprite's 9-slice border (D-025); every function takes the level's placement — one grid cell per slot (D-033) — and centres each row on its own occupied span (D-034); SlotAt is the tap, done as the inverse of the layout instead of with colliders (D-029); the one part of the view that can be wrong while looking plausible, so it is pure and tested The framing takes two reserved bands as shares of viewport height, because a board fitted to the whole screen is a board sliding under the HUD plaque and the booster bar (D-046) — height only, so a width-bound wide level does not shrink twice; PlaceColumns derives every placement from the authored one instead of editing it in place, which is what lets an undone add-column give its cell, and the row it may have started, back. ColumnMetrics reads one rule for every tray (D-048): bottom border = skirt, top = crown, the band between them = cells, and CellsInSprite is 0 when that band is not a whole number of them — TilesPerCell is how the sprite, not a prefab setting, decides between Tiled and Sliced. OrthographicSize also takes the camera's tilt, shortening what an upright board projects onto the screen's vertical axis by its cosine (width is untouched, so a wide level does not shrink twice) and refusing 90°, where the board would be edge-on (D-050). An added column takes the emptiest row that is not yet MaxColumnsPerRow wide, tie to the row above, and only when every row is full does the board gain one below — which is what makes the additions alternate up, down, up, and keeps the board as square as the limit allows (D-070). An added column is no longer given the EMPTIEST row - the rows take it in TURN, top down, one each, a row already at the limit stepped over rather than owed one later, and a new row below when a whole lap finds nowhere to stand (D-084, replacing D-070). The two agreed on an even board and disagreed on a lopsided one, where choosing the emptiest poured columns into whichever row was behind - so an addition could land at the bottom while the top row still had room. The turn is a local of the placement pass and starts at the top every time, never remembered: the placement is rebuilt from the authored one on every open and resync, so the same column count must produce the same board or undo and the boosters would drift. A one-row board needs no special case - every turn comes back to it until it is full. | h:59f0817f
Assets/Scripts/Gameplay/BoardView/BoardLayoutConfig.cs | the tunable board gaps and hud bands | sys: BoardView | api: ColumnGap; RowGap; CameraPadding; TopReserve; BottomReserve; MaxColumnsPerRow; Validate(out string) | dep: - | used: BoardView | crit: K2 | note: asset in Data/Config/ (rules/data.md); zero is legal — touching columns are a look, and a screen with no HUD reserves nothing — so only a negative value is refused, plus the one case that cannot be drawn: two reserves adding to 1 leave the board no height and would hand the camera an infinite size. The reserves are stated here rather than measured off the HUD rects, because gameplay holds no reference to UI (rules/gameplay.md) and a Canvas rect could not be unit-tested; the cost is that a HUD which grows needs these edited (D-046). MaxColumnsPerRow is how wide a row an added column may join, and it is also what the placement grid is widened to, so a three-wide level can still grow five-wide rows; a level authored wider keeps its own shape, because authored placements are never re-placed (D-070) | h:07096d51
Assets/Scripts/Gameplay/BoardView/BoardView.cs | builds the board and plays the taps | sys: BoardView | api: BoardShown; Session; ColumnCount; SelectedColumn; ColumnAt(int); Open(BoardSession,int,int,IReadOnlyList<int>); Resync(); Clear() | dep: BoardLayout,BoardLayoutConfig,ColumnView,BlockView,BlockPool,BoardInput,BoardMoveAnimator,BoardSession,BoardState,BoardColumn,BoardRules,BoardMove,CellRef,BlockSkinSet | used: AttemptStarter | crit: K1 | note: reads the attempt, never the authored level (D-014, D-015); a tap is a command and the move is committed before the tween starts; input is refused while bricks fly (D-031); no Update in this class. Resync is what a booster leaves behind — it rebuilds every column and brick from Board's state rather than reversing what it thinks changed, because an undo can restore a cover, a lock and a hidden cell at once and a shuffle rewrites every visible cell; re-reading the authority cannot drift, and it deliberately carries no animation. An added column has no authored slot, so ApplyPlacement gives it the emptiest row with room and grows a row when there is none, which re-centres (D-034) and re-frames (D-026) with no new authored number Framing and placement are both derived on every open and resync: the camera is sized to the band the HUD leaves and pushed into the middle of it, and the authored placement is kept as its own array so the grid can shrink back to it (D-046). The tilt is read off the camera, never stored: the scene owns how the board is looked at, and the position is rebuilt from the camera's own up/forward — from the distance along its view ray, which is the one measurement this method does not disturb, so framing is a fixed point instead of creeping a fraction every resync. A press is a ray against the board's plane rather than a near-plane point, which is what keeps taps on the right row once the camera leans (D-050). A tap on a column the move is illegal into hands the selection over — the lifted run drops, the tapped one lifts — rather than doing nothing, which read as a dead tap (D-055); legality is still Board's answer, only the view's reaction to a no changed. Listens to Session.ColourCompleted and dresses the finished column, and re-derives every column's settled look from BoardRules.HoldsCompletedColour on each rebuild — so undo and every booster resync are correct with no bookkeeping (D-057); it reads the animation config for those timings only. A missing animation config is reported once and loudly rather than skipped, because a settle that silently never runs reads as a bug with nothing in the console. A lifted run carries a glow: one renderer for the whole board, tinted to the run's colour, sized to its length and parented to its bottom brick — which is why it needs no per-frame code, it inherits the rock and the flight (D-060) — and taken off that brick on every path that ends a selection, because the pool re-parents a brick without its children. The glow's extent is code's, against the rule the tray and plate follow, and for the opposite reason: it depends on the run's length, which only the view knows — the reach below and above are authored numbers applied through size, never scale, so the nine-slice holds the rim while the middle stretches (D-061). The glow is the tray painted white, so its size comes from that sprite's own ColumnMetrics — a run's glow is a tray of that height, which fits the bricks by construction and needs no authored offsets — and its tint is the skin's colour mixed towards white, because an additive pass with no headroom clips the strongest channel and washes every colour to white (D-062). The glow is measured against the run rather than the tray it is drawn from, and its colour is NeonOf: the hue divided by its strongest channel, so the value rises and the hue survives, plus a small white core — mixing towards white desaturates, which is the opposite of neon (D-063). The glow's colour goes in as the shader's own `_Tint` through a property block, because neither the vertex colour nor `_RendererColor` reaches the shader on this path — measured with a diagnostic that showed the renderer holding the right pink while the screen stayed white (D-065). Open widens the placement grid to the row limit and renumbers the level's own cells into that stride: a cell is row × width + column, so the stride is what caps how wide a row can get, and widening moves nothing because each row is centred on its own occupied span (D-070). Move hands the run over topmost-brick-first — one reversal, read the same way by the targets and by the seating, so the flight and the cells cannot disagree about which brick belongs where — and hands the animator the height to cross at, read off the target column's own top plus the authored clearance (D-072). A finished colour now goes to Celebrate rather than straight to the settle, and only the live path does: a rebuilt board still lands on ShowSettled with no duration, because a column that was already finished when the level opened has nothing to celebrate (D-075). The trigger is the move, not the event: ColourCompleted fires the instant the move is legal — before the bricks have flown — so the subscription is gone and BoardMove.CompletedColours is read in ShowWhatTheMoveReported, which runs once the run is seated. It celebrates that colour's column and no other; asking every column whether it holds a finished colour sent every earlier one back into the air, because the answer is still yes (D-076). The run's bricks start their plumes as the flight starts and stop feeding them as they land, with the glow's own white lift, because the plume and the glow are the same light (D-073, D-074); the neon formula itself moved to BlockView, so there is one copy of it. Land reads the column's height before seating the run and, when the move completed no colour, fires that column's landing burst from there upward - the celebration owns the finishing landing and throws its own sparks, so an empty CompletedColours list is exactly the condition that says nothing is being celebrated (D-077). Land hands the landing burst a height and a duration rather than a speed, so the distance the config asks for is the distance travelled (D-078). The landing burst's numbers travel as one LandingLook read off the config (D-080). The crossing height now clears BOTH columns' mouths, not only the target's. That was invisible while the first leg was a diagonal - the brick drifted upward as it crossed, so it was never level with the column it was still inside - and a straight rise makes it a run turning sideways through its own upper bricks (D-081). BoardShown says the view has caught up with the board: the bricks have landed and every column this move set celebrating has finished and settled. Celebrations are COUNTED rather than assumed to be one, since a single move can finish two colours. Resync raises it at once, because a resync is instant by design (D-031) - which is also what keeps a win by shuffle from waiting for a landing that never happens (D-088). Move ends the idle rock BEFORE re-seating the source column. It already re-seated the remainder of a capped move and said so in a comment - but PlayEntry then went through Load, which calls StopIdle, which writes every rocking brick back to its LIFTED anchor: the fix was undone by something that ran after it, and only the leftover brick showed it because every other rocking brick is also a flying one whose position is overwritten each frame. The rule it keeps: the view never rearranges a brick the animator is still holding (D-090). Taps are no longer refused while bricks fly (D-098 supersedes D-031): a press that lands on a column calls animator.FinishNow() first, so the flight is COMPLETED — run seated, reveals shown, BoardShown raised — and only then does the tap touch the shared run/targets buffers. That order is the whole fix: finished later, the old landing would seat the NEW run into the OLD destination; finished on every press rather than only a served one, a tap on the background would cut a flight short for nothing. The resync path deliberately keeps SettleAndStop, since a rebuild reads every brick off Board and raises shown itself. Reveal now asks the column to turn the brick and passes the config duration; 0 or no config keeps the old instant change (D-099). Reveal builds the RevealLook here because the skin set lives here and a column has never held one; no config or no `?` skin falls back to the instant change (D-101). | h:5150155c
Assets/Scripts/Gameplay/BoardView/ColumnView.cs | one column: sized, covered, thawable | sys: BoardView | api: ColumnView.BasePlateName; ColumnView.WanderFrequency; Metrics; Capacity; BlockRoot; BrickCount; Build(BoardColumn); Celebrate(CelebrationLook,Action); SparkLanding(int,LandingLook); LandingLook.From(BoardAnimationConfig); LandingLook.Sparks; Height; Seconds; Drop; WhiteLift; CelebrationLook.From(BoardAnimationConfig); CelebrationLook.Hop; HopDuration; HopStagger; SymbolGlow; GlowHold; GlowFade; Sparks; BurstRise; BurstSeconds; SettleDuration; Shade; ShadowAlpha; BrickAt(int); Seat(BlockView); ReleaseTop(int,List<BlockView>); ForgetBricks(); ReseatAll(); Thaw(); CellPosition(int); CellLocalPosition(int); SetCoverVisible(bool); RevealAt(int,BlockSkin,RevealLook); RevealColours(RevealLook,float,out Color,out Color); RevealLook.From(BoardAnimationConfig,Color,Color,Color,Color); RevealLook.Seconds; HiddenBody; HiddenSymbol; Body; Symbol | dep: BoardLayout,BoardColumn,ColumnKind,BlockView | used: BoardView | crit: K1 | note: Tiled draw mode in one-cell steps makes capacity visible (D-007); the brick list mirrors Board and only changes from what a move reports; thawing swaps the slot sprite (D-030) Draws one divider per cell boundary from the tray's flat art, parented to the slot renderer so they share its plane and layer by construction; checks the draw mode the sprite asks for rather than assuming Tiled; and no longer forces a seated brick's rotation to identity, which is what had been overriding the Block prefab's facing every time a brick was placed (D-049). SetSettled sinks a finished column into its slot — the shadow fades in while every brick is multiplied down — on a coroutine, the pattern the move animator already uses, so no column runs an Update; a zero duration does it at once, which is how a rebuilt board is born settled (D-057). The shadow rides the tray: its size comes from the metrics and its scale and nudge are copied from the slot transform rather than authored again. The settled shadow's size follows capacity but its position and scale are the prefab's: copying them from the tray was an invention, since the shadow art has its own proportions, and it overwrote a designer's tuning on every build (D-058). Build checks the base plate and says out loud when a column has no floor — object absent, renderer off or unmaterialled, or a plate whose top is more than half a cell from cell 0's floor — because a column drawing no plate was reported and nothing in the rules or the layout can produce it; silent when the plate is where it belongs, and it owns the plate's name so the prefab tool can read it here (D-071). Celebrate is the finished column being pleased about it: one coroutine plays hop, then sparks, then the settle, because the order is the effect — the symbols burn while the bricks are up, the brightness ends with the scatter and the shadow arrives after both. Every frame writes the hop from the seated positions rather than adding to them, and the run ends by reseating, so an interruption can shorten the hop but never leave a brick off its cell (D-059); SetSettled stops a celebration in flight, since it is the authority on what a settled column looks like. The sparks come from one particle system per column, emitted at each brick's own position — a burst comes from a place rather than following one, which is sixteen systems on the board instead of a hundred and twenty-eight (D-075). The hops are staggered — bottom brick first, each a wait behind the last — and a brick's symbol lights as it leaves its cell and stays lit, so the column brightens one brick at a time and lets go all at once; the count of lit bricks is all the bookkeeping that needs, since hops start in order. The whole look arrives as one CelebrationLook rather than nine floats, where a transposed pair would mistune the board with nothing for the compiler to notice (D-076). SparkLanding is the other burst: the same one system, told to fire under every brick from a given cell upward with an upward velocity rather than outward - a brick set down on its own, with no hop and no symbol flash. The range is open-ended because bricks land on top, so everything above the column's pre-flight height is what just arrived; and no randomness is drawn in code, the spread and the fall are the particle system's own (D-077). Two burst systems now, not one, and the reason is the single thing EmitParams cannot override: gravity. The placement burst climbs and fades, the finish burst falls back, so they cannot share a system - `rise` and `finish` are separate serialized fields on separate children. Both go through Fire, which plays the system before emitting into it: that missing Play is why no spark on this board had ever been seen, since a system dressed playOnAwake=false and only ever emitted into is stopped, and a stopped system does not simulate. The finish burst moved off the bricks and onto SlotMiddle - one throw at the middle of the slot rather than one per brick, because what was finished is a colour and the column is what says so - computed from the two end cells rather than from half the column height, so it needs to know nothing about how a skirt and a crown fold into that measurement (D-078). The lit symbols now let go over FadeSymbolGlow rather than snapping, smoothstepped like the settle and ending on an exact 1 so an interrupted fade still leaves a brick at its own colour (D-059's guarantee, applied to brightness); the finish burst waits for that fade to finish, which is the moment the player calls the glow ending. Fire reports the first burst of a session - where it fired, how many particles the system then holds and what it draws with - because a burst nobody can see has exactly two explanations and one logged fact separates them, which is D-065's method rather than a fourth guess (D-079). The line is meant to be deleted once the bursts have been seen. The placement burst's five numbers became a LandingLook, reversing V-8d's own call: at three arguments a struct was ceremony, at five with three floats in a row it is exactly what D-076 wanted one for. Both bursts are now dressed with the brick's symbol mesh and lit in its own colour before they fire. The mesh is the RENDERER's, not the particle's - Unity draws one mesh per system - which is exact here because a landing run is one colour and a finished column is one colour; the one thing it cannot express is two live bursts of different colours in one column at once, and that is written down rather than left to be found. A skin with no spark mesh falls back to the billboard it used to be and says which command fixes it, because this series has already spent three rounds on effects that were silently absent (D-080). Both looks carry their spark's size now, applied through EmitParams so one system still serves every burst. The finish burst spans the MIDDLE TWO CELLS rather than one point - two throws of half the count each, and one throw when an odd column's middle is a single cell - so it reads as the column letting go rather than as something happening at a spot on it. And the placement burst decelerates: it leaves at twice its average speed and Slow() gives it a pull of 2h/t squared, which stops it exactly at the authored height exactly as it fades, instead of one flat speed cut off at the end. That pull is a system property like the mesh, so a burst still alive is re-tuned by the next one; and with the project's gravity at zero the sparks keep the flat rise rather than dividing by it (D-081). The two bursts stopped being dressed and became DRIVEN: Shape, Wander and Climb write the emission region, the noise and the whole motion per burst out of the config, beside the mesh, colour, size, lifetime and velocity already written there. That moves D-053's line rather than abandoning it - the tool creating a look once and a designer owning it is right wherever the designer can reach it, and every number asked for over three rounds lived on a prefab and cost a rebuild to change (D-082). Climb is a velocity CURVE rather than a speed and a pull, because 'down a little, then up, then softly nothing' is three phases and an acceleration is one; its area is measured and divided out, so the authored climb survives any dip. All three velocity axes are written in one curve mode, which is D-078's console fault met at the source instead of repaired after. The finish burst throws once out of an area spanning the middle two cells - its height measured off the column rather than authored - on a computed arc that puts its apex at the halfway mark. And FadeSymbolGlow is the one place in this project that must NOT smoothstep: that curve's slope is zero at the end, so its last third is invisible and the burst waiting on it arrived into a silence. The finish's fade and its burst now OVERLAP rather than queueing - the light starts going out as the column comes together, the sparks come out a lead into that, and the two end together - which is the third shape this moment has been given and the first that reads as one event. Still one coroutine, D-075's point exactly: two would need synchronising rather than being written down in order, so the fade is the loop the burst fires inside and FadeSymbolGlow is absorbed into it. Band/Shift give every axis a PAIR of curves rather than one, so Unity draws each particle its own place between them and no two sparks travel together; the band is centred on the climb, which is what keeps the authored height as the burst's average. Always a pair even at zero spread, because three axes in disagreeing curve modes is D-078's console fault - avoided at the source rather than repaired after (D-083). Celebrate takes a callback for when the column has hopped, burst and finished sinking into its slot - called on every path including the ones that skip the hop, or a caller counting celebrations would wait forever for one that never started. The settle is a coroutine of exactly SettleDuration, so waiting that out is the same number read from the same look rather than a guess (D-088). RevealAt turns a `?` brick over instead of changing it between frames: the `?` face swings away, the skin changes at ±90 where that face is edge-on and invisible, and the real face swings in from the same side — so the cube side that was showing has become the real front, which is the effect and not an artefact (no scale is touched, deliberately). Half a turn and not a quarter is forced: the symbol is embossed on one face and the prefab already points it at the camera (D-047), so only the two ends show a symbol. It takes a float, not a Look struct — one number does not earn one (abstraction-level.md). SettleReveal is the interruption contract and every path that takes a brick out of this column calls it (ReleaseTop, ForgetBricks, Build, SetSettled), because a turn writing a rotation while the animator writes the position would fight over one transform — reachable since taps stopped waiting for flights (D-098, D-099). D-099's turn is GONE (D-101): the reveal no longer touches the transform at all. The `?` dissolves into the body colour while that colour travels to the real one, the skin is swapped where the symbol has no contrast left, and the real symbol emerges from the body. Contrast is the only thing hiding the swap because the symbol is embossed geometry — and it hides it imperfectly, since an embossed shape in its body colour still catches light as a faint relief (D-052); that is the accepted price of a straight change, transparency being the only alternative and costing the brick materials their batching. RevealColours is pure and static so the property the whole effect rests on — symbol equals body at the midpoint — is testable without a scene. Interruption got SIMPLER than the turn's: no pose to restore, only painted colours to hand back. | h:0e097b4c
Assets/Scripts/Gameplay/BoardView/BlockView.cs | one brick, skinned at spawn | sys: BoardView | api: Skin; Apply(BlockSkin); SetShade(float); Neon(float); SetSymbolGlow(float); StartPlume(float,float,float,float); StopPlume(); ClearPlume(); NeonOf(Color,float); PaintReveal(Color,Color); ClearReveal() | dep: BlockSkin | used: BoardView,BlockPool | crit: K2 | note: one Block prefab for the whole game (D-004); shared mesh and material on purpose, since touching .material would clone twelve materials into a hundred Assigns both material slots from a two-element array held as a field — body first, symbol second, matching the mesh's slot order — still through sharedMaterials, so nothing clones (D-004, D-052). SetShade multiplies both material slots through one shared MaterialPropertyBlock and clears it to restore exactly, so a settled brick darkens without cloning a material (D-004, D-057); Apply clears the shade too, because a property block outlives the pooled brick it was set on. Carries the plume a flying brick leaves: a ParticleSystem on its own child, since one object holds one renderer, emitting by *distance* so the brick's own movement paints the corridor and a brick waiting its turn lays nothing, simulating in world space so a puff stays where it was dropped. Its colour is the system's start colour, so one material serves twelve brick colours with nothing cloned. Stopping stops the emission and the puffs fade where they are; Apply clears them outright, or a pooled brick would leave a plume over a board nobody moved. Lifetime, size and density come from the config, next to the flight they must agree with; the noise, the fade curves and the puff texture are the prefab's and the art's (D-053, D-074). SetSymbolGlow is the same property-block mechanism with the multiplier above 1 and only the symbol's slot written, so the shape in the middle burns while the body keeps its colour — the overshoot is what bloom turns into light (D-066, D-075). NeonOf lives here now: two things are lit by it, the glow and the trail, and the formula is not worth having two copies of (D-063). A brick remembers the skin it was dressed as and answers for it, which is how a burst gets the symbol to be shaped like without the board threading it down through every call that already knows which brick it means. Neon(float) is the colour-as-light formula behind one accessor now, with StartPlume calling it rather than keeping a second copy (D-063, D-073, D-080). PaintReveal writes the two slots OUTRIGHT rather than scaling what the materials hold, because a reveal walks between two different SKINS and there is nothing to scale from; ClearReveal hands both back, exact for the same reason SetShade at 1 clears instead of restoring (D-101). | h:f2dea295
Assets/Scripts/Gameplay/BoardView/BlockPool.cs | brick pool for the board | sys: BoardView | api: BlockPool(BlockView,Transform); IdleCount; CreatedCount; Take(); Return(BlockView) | dep: BlockView | used: BoardView | crit: K2 | note: rules/gameplay.md requires a pool; plain C# class, never a scene component | h:b34036ed
Assets/Scripts/Gameplay/BoardView/BoardInput.cs | turns a press into an event | sys: BoardView | api: Pressed | dep: - | used: BoardView | crit: K2 | note: Pointer.current covers mouse and touch in one path; per frame it is one null check and one boolean, and the position is read only on a press; it knows nothing about columns. A press that lands on UI is refused, or the tap opening Pause would also lift a brick behind it (D-037) — asked as an EventSystem raycast at the press position rather than IsPointerOverGameObject(), which answers for whichever pointer the module last processed and needs a pointer id to be right about touch; buffers are fields, so it allocates nothing | h:487cf278
Assets/Scripts/Gameplay/BoardView/BoardAnimationConfig.cs | the move's feel, as data | sys: BoardView | api: LiftHeight; LiftDuration; TravelDuration; DropDuration; EntryClearance; EntryStagger; EntryTravelShare; LiftTiltDegrees; LiftTiltCycles; IdleTiltDegrees; IdleTiltPeriod; GlowPadding; GlowBelow; GlowAbove; GlowNudgeX; GlowLift; CelebrationGlowFade; CelebrationBurstRise; CelebrationSparkSize; CelebrationBurstSpread; CelebrationBurstScatter; LandingSparkSize; LandingSparkScatter; LandingSparkSpread; LandingSparkWander; LandingSparkDip; CelebrationBurstSeconds; LandingSparks; LandingRiseHeight; LandingRiseSeconds; LandingSparkDrop; SettleDuration; SettleShade; SettleShadowAlpha; Validate(out string); RevealFadeDuration | dep: - | used: BoardMoveAnimator | crit: K2 | note: asset in Data/Config/ with no field defaults, so a fresh asset is invalid and says so rather than shipping a guessed feel (rules/data.md) Also carries the settle look — duration, brick shade and shadow alpha — validated at both ends, since a shade of 0 is a black brick and 1 is no effect at all. Also carries how a lifted run rocks — the rise's tilt in degrees with a whole-number cycle count, and the idle rock's degrees and period; a tilt at or past 90° is refused, as is a fractional cycle, which would land the run tipped. Plus glowPadding: how far past the lifted run its light reaches. The glow's two dials are now one outset and one lift towards white; the old reach-below/above pair went away with the shadow-shaped glow (D-062). The glow's dials are the outset, the reach below and above, a sideways nudge for a drawing that is not quite centred, and a small white lift (D-063). arcHeight became entryClearance — how far above the target column's top a brick crosses — carried across by FormerlySerializedAs rather than replaced, because the arc it described no longer exists; entryStagger and entryTravelShare join it, and the share is refused at 0 and 1 since what is left of the flight is the drop (D-072). trailTime, trailWidth and trailDensity join them — a puff's lifetime, its size in cells and how many go into one cell of travel — beside the travel duration they have to agree with, while the plume's noise and fade curves stay the prefab's (D-073, D-074). Density rather than a rate per second is what keeps the plume the same however fast the brick moves. The celebration's four — hop height, hop seconds, symbol glow and sparks per brick — join them, with the glow refused under 1 because it multiplies the symbol's colour and would darken what is meant to light up (D-075). celebrationHopStagger and celebrationGlowHold join them; the shadow's fade and the bricks' darkening still come from the one settleDuration in one lerp, so they agree by construction rather than by two numbers kept in step (D-076). landingSparks, landingSparkRise and landingSparkDrop join them: how many sparks a landing that finished nothing throws under each brick, the upward push they leave with and how far below the brick's middle they start. Zero is legal on all three, unlike the durations - an asset authored before the fields existed reads them as 0, and refusing that would report the whole move invalid over a number nobody set (D-077). The landing burst's speed became a height plus a duration - landingRiseHeight and landingRiseSeconds replace landingSparkRise outright, with no FormerlySerializedAs, because the value was one task old, never tuned, and changed units: carrying it across would import a velocity as a height. The finish burst gains celebrationBurstRise and celebrationBurstSeconds, a speed rather than a height because the fall is the prefab's gravity and only one of the two belongs in this asset. Zero stays legal everywhere except a duration with particles behind it - the landing's speed is height divided by seconds, and a spark with no lifetime is one nobody sees (D-078). celebrationGlowFade joins them - how long a lit symbol takes to come back down to its own colour. It is not only a look: the finish burst waits for it, because 'when the glow ends' is the moment asked for and a fade moves that moment. Zero is the snap this did before it was a number (D-079). The two spark sizes join it, because they are the numbers the bursts are judged on and judging them must not need a prefab rebuild. entryTravelShare is GONE with no FormerlySerializedAs, unlike arcHeight before it: that number kept its job and lost its name, this one described a two-leg flight that no longer exists, and nothing it meant survives. landingRiseHeight is still exact by different arithmetic - the rise decelerates now rather than carrying no gravity (D-081). Four more join it - the finish burst's area, and the placement burst's area, wander and dip - because they are what the effect is judged on and every one of them used to live on a prefab where changing it cost a rebuild (D-082). The dip is refused at or past 1: it is a share of the climbing speed, and the climb's length is the motion curve's area divided out of the authored height, so a dip that matches the climb collapses that area towards nothing and then flips its sign - the sparks would sink from a number that reads like a harmless 'a bit more'. Two scatters join it and the headers are regrouped so HOW MANY, HOW BIG AN AREA and HOW FAST sit together at the top of each burst - finding a dial should not need the person who wrote it. celebrationGlowHold kept its name and place and CHANGED WHAT IT MEASURES: it was how long the symbols stayed lit before the fade began, it is now how long that fade has been running when the burst fires. And landingRiseHeight stops being exact: it is the average now, because scatter is sparks going different distances and that is the thing that was asked for (D-083). revealTurnDuration is how long a `?` brick turns for, the whole turn and not one half. Deliberately NOT refused at 0: this asset refuses zero only where zero fails silently, and zero here is "no turn, change the brick" — a legal look and what an asset written before the field carries (D-099). revealTurnDuration was RENAMED to revealFadeDuration when the turn became a colour change (D-101); the asset value and the test constant moved in the same task, because renaming a serialised field is how an authored value silently becomes 0. | h:9486cdae
Assets/Scripts/Gameplay/BoardView/BoardMoveAnimator.cs | tweens bricks between committed states | sys: BoardView | api: BrickMotion.Lift,Drop; IsBusy; LiftHeight; Play(IReadOnlyList<BlockView>,IReadOnlyList<Vector3>,BrickMotion,Action); PlayEntry(IReadOnlyList<BlockView>,IReadOnlyList<Vector3>,float,Action); EntryPoint(Vector3,Vector3,float,float); PlayIdle(IReadOnlyList<BlockView>,IReadOnlyList<Vector3>); StopIdle(); SettleAndStop(); FinishNow() | dep: BoardAnimationConfig,BlockView | used: BoardView | crit: K1 | note: starting a motion settles the previous one, so an interruption can never leave a brick mid-air or disagree with Board (rules/gameplay.md); buffers are fields because this is per-frame code; snaps with a loud error when the timings are unauthored A rising run rocks once and lands level, and a lifted one keeps rocking: the whole stack tips about its own centre — top-left corner out, then top-right — rather than sliding sideways (D-059). The roll is applied in front of each brick's own rotation so the 180° that faces its symbol survives, and Settle puts rotations back as well as places, since nothing else in the view writes a rotation. The rock is a second coroutine with its own anchors and anchor rotations, computed from those every frame so it cannot accumulate; it leaves IsBusy alone, because the tap that hands the selection over has to land mid-rock. Travelling is no longer one of the motions: PlayEntry sends the bricks one at a time, each starting a stagger after the one before, along a path that crosses to above the target column and only then drops in — so a brick enters through the column's mouth instead of sliding through its wall. The path is a pure static EntryPoint, tested without a scene because a path that ends anywhere but on the cell leaves a brick off the board; it raises its crossing height to clear both ends, so a move into a lower row cannot sag through the board (D-072). The entry path became THREE STRAIGHT LEGS - up out of its own column, across at the apex, down through the target's mouth - because the old one drove x and y from a single eased value, so a brick left its column diagonally and arrived at the slot from the side. Time is split in proportion to each leg's own length, so the brick holds one speed all the way round and the shape needs no authored share: entryTravelShare is deleted, since what it described no longer exists. One easing over the whole journey rather than per leg, or it would stop dead at every corner (D-081). Stopping a flight and COMPLETING one are now two methods, because they are two intentions: SettleAndStop places the bricks and ABANDONS the callback (teardown and resync, where a rebuild reads every brick back off Board anyway), FinishNow places them and RUNS it. The callback moved off the coroutine parameter into a pending field so FinishNow can reach it, and Complete clears it before invoking so it fires exactly once whichever path gets there first. Load warns rather than dropping a pending completion silently: a dropped landing is a brick standing on the right cell that no column owns (D-098). | h:d280c510
Assets/Scripts/Gameplay/Board/BoardSolver.cs | editor-only solvability search | sys: Board | api: SolverVerdict.Solvable,Unsolvable,NotProven; SolverMove(int,int); SolverMove.From; To; SolverResult(SolverVerdict,int,IReadOnlyList<SolverMove>,string); SolverResult.Verdict; StatesExplored; Solution; Summary; BoardSolver.DefaultStateBudget; MaxDepth; Solve(LevelData,int) | dep: BoardSession,BoardState,BoardColumn,BoardMoveHistory,LevelData,AttemptScramble,BlockColourId | used: LevelEditorWindow | crit: K2 | note: the level editor's second gate (D-010) — Validate() proves a board is legal, this proves it can be won; #if UNITY_EDITOR keeps it out of every build while leaving it where its tests can reach it (D-035); drives a real BoardSession rather than re-implementing the move rule; depth capped at BoardMoveHistory.MaxEntries because that is exactly how far Undo can walk back; exact string state keys, and the only reduction beyond legality is the identical-empty-column symmetry, because a false "unsolvable" is the one answer it must never give | h:237c0c41

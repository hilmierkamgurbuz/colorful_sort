<!-- stamp: 3c415f0 2026-08-21 status: OK -->
Assets/Scripts/Content/DifficultyLabel.cs | authored per-level difficulty label | sys: Content | api: Normal; Hard; SuperHard | dep: - | used: LevelDefinition | crit: K3 | note: authored, never derived (reference §4); the displayed strings belong to UI | h:cbc02911
Assets/Scripts/Content/ColumnDefinition.cs | serialized column and cell rows | sys: Content | api: CellDefinition.colourId; CellDefinition.hidden; ColumnDefinition.Kind; Capacity; ThawAfterCompletions; CoverKeyColourId; Cells; ToColumnData() | dep: LevelData,ColumnKind,BlockColourId | used: LevelDefinition | crit: K2 | note: carries no validation of its own — ColumnData owns those rules Gains an internal Create for the codec, for the same reason and with the same reach as LevelDefinition's (D-085). | h:ca9e8b2b
Assets/Scripts/Content/LevelDefinition.cs | authored level asset and conversion | sys: Content | api: LevelDefinition.FirstLevelIndex; LevelDefinition.Create(int,DifficultyLabel,int,int,ColumnDefinition[],int[]); LevelIndex; Difficulty; LayoutRows; LayoutColumns; Columns; LayoutCells(); ToLevelData(); Validate(out string) | dep: ColumnDefinition,DifficultyLabel,LevelData,BoardState | used: LevelDatabase,AttemptStarter,LevelEditorWindow | crit: K2 | note: layout and label stay this side of the fence; Validate() is the level editor's shippability check (D-010) and also refuses a placement that puts two columns in one grid cell; LayoutCells() is per-slot placement, empty meaning the plain fill (D-033) It is still a ScriptableObject and there is still no level asset: levels are decoded into TRANSIENT instances, created and never saved, marked HideAndDontSave. That is what let the level editor keep its whole SerializedProperty surface unchanged (D-032 survives) while level assets went away - the alternative, a plain C# class, would have rewritten a thousand-line window and rippled through three systems for nothing the change needed. Create is internal, so only Content can make one: the file is the authority for level content and a public setter would be a second way to write it (D-085). Levels are numbered FROM 1 and Validate refuses anything below it. Nothing computes that - progression counts ordinals and the database never renumbers (D-085) - so the validator is the only place the rule can be true rather than hoped (D-086). | h:7e901342
Assets/Scripts/Content/BlockSkin.cs | one colour's mesh and material | sys: Content | api: SymbolMesh; Material; UiColour; IsAssigned | dep: - | used: BlockSkinSet | crit: K3 | note: holds no colour id — the mapping is the set's job (D-003) Carries a second material for the engraved symbol's own mesh slot; a skin without one is refused by the set, because one material paints the symbol the brick's colour and it reads as nothing but a shadow (D-052). sparkMesh joins it: the symbol on its own, generated from the brick mesh, for the bursts to be shaped like. A second field rather than a rename - SymbolMesh is, despite its name, the whole brick - and left out of IsAssigned on purpose, because a brick with no spark mesh still draws perfectly and refusing to spawn it would turn a missing effect into a missing board (D-080). | h:b6fbe2dc
Assets/Scripts/Content/BlockSkinSet.cs | the single colourId to look mapping | sys: Content | api: Count; HiddenSkin; TryGetSkin(BlockColourId,out BlockSkin); GetSkin(BlockColourId); Validate(out string); HiddenSymbolColour | dep: BlockSkin,BlockColourId | used: BlockSkinFactory | crit: K2 | note: the one place a re-skin happens (D-003); BoardView reads it at block spawn; the ? brick is one slot, not a colour row (D-021); Validate refuses a material whose base colour disagrees with the skin (D-020) Also owns symbolShade and SymbolColour(skin): the symbol's colour is derived from the skin's own rather than authored twelve times, which is the same single-authority argument that put the colour map here (D-052); Validate refuses an unauthored shade and checks each material against the colour it is supposed to carry — body against the skin's, symbol against the darkened one. The `?` mark is the ONE authored symbol colour, and on arithmetic rather than taste: symbolShade multiplies so it can only darken, and the `?` is cut into a near-black brick where darker is invisible. It sits here and not on BlockSkin because a field there would be twelve authorable symbol colours — the drift D-052 closed — while here it is one value for the one slot that was never a colour row (D-021). SymbolColour answers for both cases so the generator and Validate cannot disagree, and Validate refuses transparent or black, which is what an unset Color holds (D-099). | h:a26513b1
Assets/Scripts/Content/LevelDatabase.cs | ordered level list for progression | sys: Content | api: MaxLevels; Count; Reload(); Levels; ByOrdinal(int); TryFindByLevelIndex(int,out LevelDefinition); Validate(out string) | dep: LevelDefinition | used: AttemptStarter,LevelEditorWindow,BoardViewPrefabFactory | crit: K2 | note: Meta progresses by ordinal, the plaque shows LevelIndex; per-level validity is each level's own Validate. Its asset is rebuilt from the level folder by the level editor's Rebuild database button — a button and not a runtime scan, because a build has no folders to scan and the asset that lists its levels is the thing that ships It holds a TextAsset now rather than an array of level assets - a normal asset reference, so no Resources/ and no string path, and the blueprint's standing refusal of both survives. The file is read once into rows and a level is BUILT ONLY WHEN IT IS PLAYED, then kept: two thousand rows cost a parse, two thousand levels would cost two thousand ScriptableObjects at boot. TryFindByLevelIndex searches the rows rather than the levels, so looking for level 79 does not build the seventy-eight before it - which is why the index is a key in the file. A failed read is remembered as failed, or a broken file would report itself on every call for the rest of the session (D-085). | h:ca97d6bc
Assets/Scripts/Content/ColorfulSort.Content.asmdef | authored data assembly | sys: Content | api: asmdef ColorfulSort.Content | dep: ColorfulSort.Board | used: ColorfulSort.BoardView | crit: K1 | note: makes the blueprint's Content -> Board arrow compiler-enforced and is what lets BoardView reference Content at all (D-024) | h:9ddd7ce8
Assets/Scripts/Content/SymbolMeshBuilder.cs | lifts a symbol out of a brick mesh | sys: Content | api: SymbolMeshBuilder.SymbolSubMesh; Build(Mesh,string,out string) | dep: - | used: BlockSkinFactory | crit: K3 | note: the symbol is not a sprite and never was - it is submesh 1 of the brick mesh, which is why a skin carries a second material for it (D-052). Baked at authoring time into its own asset, so nothing here runs in a build. Three transforms are baked in and each is a FACT about how the brick is drawn: a half turn about Y, because the Block prefab carries exactly that so its symbol faces the camera and a particle mesh is given no transform at all (without it every spark is mirrored and back-turned); recentred on its own bounds, because a particle draws around its position while the symbol sits on the brick's face; and scaled so its widest side is one unit, so a particle size means the same for all twelve and survives a re-skin. A half turn is a proper rotation, so the winding survives and no triangle is re-ordered. It lives in Content behind UNITY_EDITOR rather than under Assets/Editor/ for D-035's reason: an editor folder compiles where no test assembly can reach it, and a triangle list remapped one index out still produces a mesh - just the wrong one (D-080). | h:3a4e26f9
Assets/Scripts/Content/LevelCodec.cs | the level file's format, both ends | sys: Content | api: LevelCodec.Version; LevelRow.i,d,r,c,p,k; TryReadFile(string,out LevelRow[],out string); TryDecode(LevelRow,out LevelDefinition,out string); TryDecodeAll(string,out List<LevelDefinition>,out string); Encode(IReadOnlyList<LevelDefinition>); TryDecodeColumn(string,out ColumnDefinition,out string) | dep: LevelDefinition,ColumnDefinition,BlockColourId,ColumnKind | used: LevelDatabase,LevelEditorWindow | crit: K1 | note: levels are not assets - they are lines in one compact JSON file, and this is BOTH ENDS of it, so a writer and a reader in two files cannot drift apart. A column packs into one string, <kind><capacity>[:cells][#thaw][/cover], kinds n i v m, a letter per colour (a..l for 1..12, . for none) and * before a hidden cell - which is where nearly all the size went, since a four-cell column was six lines of YAML and is six characters now. The three optional sections are read in any order and written in one. TryReadFile stops at the ROWS rather than building levels: two thousand rows are small structs where two thousand levels would be ScriptableObjects and their columns, and that split is the whole load story. Version is the first key and a decoder refuses a version it does not know, because reading a thirteenth colour as something it recognises is worse than failing. Text on purpose over gzip: a level that decodes to the WRONG board still plays, so reading a diff is not convenience here (D-085). | h:6962e887

<!-- stamp: a082d5f 2026-08-21 status: OK -->
Assets/Scripts/UI/ColorfulSort.UI.asmdef | screen ui assembly definition | sys: UI | api: asmdef ColorfulSort.UI | dep: ColorfulSort.Content,ColorfulSort.Core,ColorfulSort.Meta,UnityEngine.UI,Unity.TextMeshPro | used: - | crit: K1 | note: the blueprint's UI arrow made compiler-enforced (D-036), which is what forced Meta to get an assembly too — an asmdef cannot reference Assembly-CSharp, so AttemptStarter was unreachable without one; Board is deliberately absent until 4B's Win/Fail need a session | h:2658191f
Assets/Scripts/UI/UiStyleConfig.cs | text style and runtime-chosen copy | sys: UI | api: TextFill; TextOutline; TextShadow; OutlineWidth; ShadowOffset; ShadowSoftness; PlaqueFor(int); MenuLevelFor(int); MenuNoLevel; LabelFor(DifficultyLabel); BoosterTitleFor(BoosterId); BoosterBlurbFor(BoosterId); BuyLabelFor(int); CoinAmount(int); CoinReward(int); Validate(out string) | dep: DifficultyLabel,BoosterId | used: GameplayHud,CoinHud,BoosterShopPopup,WinPopup,UiFactory | crit: K2 | note: asset in Data/Config/ with no field defaults, so a fresh one is invalid and says so (rules/data.md); holds only copy that cannot be baked - the difficulty word is picked from an enum, the plaque from a number, and the booster shop's three titles and blurbs because ONE popup prefab serves all three boosters - while a popup's fixed title is baked into its prefab instead, or the config would be a second authority. The TMP material is generated from these colours the way a BlockSkin's material is generated from its own (D-020). The menu's level button gets its own wording beside the plaque's, upper-cased in the ASSET rather than by a ToUpper in code (D-020, D-086), and it owns what that button reads with no levels, because a blank button reads as broken. coinRewardFormat is what the win popup says it paid: the NUMBER is Meta's LastWinAward and only its writing is here, which is why the plus sign is authored rather than concatenated. The five coin-flight numbers and the counter's two durations are GONE with the flight itself - a config field nothing reads is a number a designer will tune and watch do nothing (D-092) | h:c0a0eafa
Assets/Scripts/UI/SafeAreaPanel.cs | pins a rect to the safe area | sys: UI | api: - | dep: - | used: - | crit: K2 | note: rules/ui.md refuses positioning by where the background art lands, because 19.5:9 and 20:9 crop it; recomputes on OnEnable and OnRectTransformDimensionsChange, never in Update, and the applied-area comparison is what stops its own anchor write from recursing | h:9955e959
Assets/Scripts/UI/Popup.cs | base popup, close request only | sys: UI | api: CloseRequested; Close(); OnOpened(); OnClosing() | dep: - | used: PopupHost,PausePopup | crit: K2 | note: a base class because the blueprint names four popups, which is the repetition abstraction-level.md asks to see before extracting; a popup never destroys itself — the host does, or the stack would hold a dead entry | h:2dd408e6
Assets/Scripts/UI/PopupHost.cs | the only thing that opens popups | sys: UI | api: Instance; AnyOpen; Open<T>(T); Close(Popup); CloseTop() | dep: Popup | used: GameplayHud | crit: K1 | note: persistent on Boot's --UI-- root because DontDestroyOnLoad keeps roots only, and Screen Space - Overlay needs no camera so it survives the additive Menu/Game swap; its canvas sorts at 200 above the HUD's 100, so "on top" is a canvas fact and not an ordering accident; the scrim rides one sibling under the top popup and is what makes the screen behind it untappable (D-037) | h:2c522803
Assets/Scripts/UI/SettingToggleButton.cs | one round sound, music or vibration toggle | sys: UI | api: Setting.Sound=0,Vibration=1,Music=2 | dep: GameRoot | used: UiFactory | crit: K2 | note: one component for every round toggle — three in Pause, and Settings later — which is why rules/ui.md asks for it shared; the enum is numbered explicitly and only ever appended to, because every popup prefab carries those ints and a value inserted in the middle silently turns one button into another setting's (D-045); state is drawn as an icon PAIR, onIcon/offIcon with one Image.enabled flip each, since the pack ships the crossed-out variant as its own sprite and the earlier single-icon-plus-slash made "off" two graphics that had to line up; enabled flips rather than SetActive because this runs on every click and open and a disabled graphic costs no layout work; holds no bool of its own — it writes through GameRoot, the toggles' single writer (fingerprint.md), and re-reads what it just set, because a local mirror is how a settings screen ends up disagreeing with the game | h:780b85f9
Assets/Scripts/UI/PausePopup.cs | the gear's popup and its commands | sys: UI | api: Bind(AttemptStarter) | dep: Popup,AttemptStarter,GameRoot | used: GameplayHud,UiFactory | crit: K2 | note: performs nothing itself — restart is a command to Meta, quit is a command to Core (rules/ui.md); the restart target is injected by the screen that opened it, because a popup under Boot's persistent canvas has no honest way to find an object in the Game scene; Restore Purchase and Contact Us are dropped on purpose, having nothing behind them | h:d256a3e8
Assets/Scripts/UI/GameplayHud.cs | the gameplay screen's ui, popups and booster bar | sys: UI | api: - | dep: UiStyleConfig,PopupHost,PausePopup,WinPopup,FailPopup,BoosterShopPopup,BoosterButton,AttemptStarter,LevelDefinition | used: UiFactory | crit: K2 | note: renders from AttemptStarter.AttemptOpened and never polls; reads OpenedLevel on enable as well, because an event announces a change and something enabled later needs the state; owns which popup opens when - the gear opens Pause, AttemptWon/AttemptDeadlocked open Win and Fail, taken from Meta rather than from Board so no session reference is needed (D-038), and an empty booster's ShopRequested opens the shop here rather than in the button, which holds no prefab of its own. It binds the three booster buttons and refreshes them on BoardChanged and on EconomyChanged - a purse moves for reasons a board knows nothing about, a purchase being the case that needs it. It shows NO balance: the only counter in the game is in the shop, where it can be spent (D-092), and the win popup is handed the style config because what a level paid is a sentence this screen's asset knows how to write. Every one of those needs Boot loaded, since the host and the EventSystem both live there; a scene object rather than a prefab (scene-structure.md) | h:9e2f08ad
Assets/Scripts/UI/WinPopup.cs | the solved-level popup | sys: UI | api: Bind(AttemptStarter,UiStyleConfig) | dep: Popup,AttemptStarter,UiStyleConfig | used: GameplayHud,UiFactory | crit: K3 | note: one button and deliberately no close cross - dismissing it would leave the player on a finished board with nothing to do; the button asks Meta for the next level and nothing else, because whether there IS one is progression's answer decided before this popup opens (rules/ui.md). It now says what the level paid - a coin and +20 - and that is READ, never decided: Meta banked the coins the moment the level was solved and this only writes the number down, formatted by the style config (D-092). A replayed level pays nothing and the row goes AWAY rather than reading +0, which would look like a bug in the payout. The RewardAnchor the coin flight used is gone with the flight | h:17e838f2
Assets/Scripts/UI/FailPopup.cs | the no-legal-move popup | sys: UI | api: Bind(AttemptStarter) | dep: Popup,AttemptStarter,GameRoot | used: GameplayHud,UiFactory | crit: K3 | note: retry is a command to Meta and quit a command to Core, like every other popup (rules/ui.md); it spends nothing — what a deadlock costs is OPEN-3 and belongs to Meta's economy in task 5, and a popup spending a currency nobody owns yet would be UI deciding progression policy; no close cross, since the board underneath cannot be played | h:4e0c3b31
Assets/Scripts/UI/MainMenu.cs | the menu's one button, the route in | sys: UI | api: - | dep: GameRoot | used: UiFactory | crit: K2 | note: without it nothing Boot carries is reachable — the popup host and the project's single EventSystem both live there, which is why the HUD's gear was silent and no popup could open when the Game scene was entered on its own; shows no coins, hearts or progress bar because those read Meta's economy, which does not exist yet, and a counter reading zero is decoration standing in for a system; asks Core for a screen and stops there (rules/ui.md), with no double-press guard because SceneFlowService already refuses a request that arrives mid-swap The button says which level it opens - LEVEL 1 - worked out from the save's ordinal and the database, because AttemptStarter is in the Game scene and does not exist while the menu is up. Read once as the screen comes up: the ordinal only moves when a level is finished, which means leaving this screen. With no levels it says so and goes disabled rather than opening a Game scene with nothing to build a board from - a designed state, since it is where the project sits before its first level and where a mis-typed level file puts it later (D-086). | h:208a7a09
Assets/Scripts/UI/BoosterButton.cs | one of the three gameplay boosters | sys: UI | api: ShopRequested; Booster; Icon; Bind(AttemptStarter); Refresh() | dep: AttemptStarter,BoosterId | used: GameplayHud,UiFactory | crit: K2 | note: one component for all three, because the blueprint plans one prefab with three instances and the only difference is which command they send; costs nothing to press (D-042, D-043) and counts no charge; decides no legality of its own — CanUndo/CanAddColumn/CanShuffle come from Board through the attempt seam, so the greyed-out button and the refused command cannot disagree; refreshed on an event, never in Update Its own Booster enum is GONE, replaced by Meta.BoosterId declared in the same order - the three instances serialize those ints, so the order is the migration and an inserted value would turn Undo into Shuffle (D-045). It carries a charge now: a red count badge while there are charges and a green plus at zero, and the press follows the badge - command with charges, ShopRequested without. Empty is always interactable, because what it opens is a shop rather than a move; with charges it is still Board's legality that greys it out. It spends nothing and prices nothing - the charge is taken in Meta after Board accepts the mutation (D-091, rules/ui.md). | h:1e05cc89
Assets/Scripts/UI/CoinHud.cs | the shop's coin pill | sys: UI | api: Show(int) | dep: UiStyleConfig | used: BoosterShopPopup | crit: K3 | note: it is NOT screen furniture - the gameplay HUD and the menu deliberately show no balance, because a counter the player cannot spend from is decoration, so the only pill in the game sits at the booster shop's top-left over the host's scrim (D-092). Holds no balance and animates nothing: the purse is Meta's and this is handed a number (rules/ui.md), and the climb it used to do belonged to the coin flight, which went with it - a counter with nothing to count in is a coroutine kept for a feature that no longer exists | h:50e64228
Assets/Scripts/UI/BoosterShopPopup.cs | the popup behind a booster's plus | sys: UI | api: Bind(AttemptStarter,UiStyleConfig,BoosterId,Sprite) | dep: Popup,AttemptStarter,BoosterOffer,BoosterId,UiStyleConfig,CoinHud | used: GameplayHud | crit: K3 | note: ONE prefab for all three boosters, which is why its title, its blurb and the buy caption come from UiStyleConfig while the fixed furniture stays baked into the prefab - copy chosen at runtime is config (D-020, rules/ui.md). It sells nothing itself: the price, whether the purse covers it and what a purchase does to the save are Meta's answers through the attempt seam, the same line WinPopup does not cross when it asks for the next level. The icon is handed over by the BoosterButton that opened it rather than looked up, so a booster's picture stays in the one place the user dressed (D-071). It also CARRIES the game's only coin pill, at its top-left: this is the one screen where a balance can be spent, so it is the one screen that shows it (D-092). No rewarded-ad half on purpose - there is no ad system (fingerprint.md), and a button with nothing behind it is what Pause dropped Restore Purchase for. A refused buy re-renders instead of closing, or a live button would go on failing silently | h:c934ad32

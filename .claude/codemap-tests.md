<!-- stamp: 3c415f0 2026-08-21 status: OK -->
Assets/Tests/Board/BoardRulesTests.cs | move win deadlock rule tests | sys: Board | api: TestBoards.Cell(int,bool); TestBoards.Normal(int,int[]); TestBoards.Ice(int,int); TestBoards.Covered(int,int,int[]); TestBoards.Mystery(int,int[]); TestBoards.Level(ColumnData[]); TestBoards.Session(ColumnData[]); TestBoards.VariantSession(int,ColumnData[]); TestBoards.Fingerprint(BoardSession) | dep: BoardSession,AttemptScramble,BoardRules,BoardState,BoardColumn,LevelData,BlockColourId | used: BoardUndoTests,ColumnModifierTests,AttemptScrambleTests | crit: K3 | note: also holds TestBoards; Session() builds the authored board via AttemptScramble.None so a fixture means what it says Adds the finished-colour lock: reached by playing rather than authored (a level may not start with a colour gathered), a full mixed column still lifting, a colour shorter than its column still counting as finished, the lock arriving with the completing move, and the two end-state cases — the lock never takes the last move away, and an all-finished board reads as won rather than deadlocked. | h:1bbb71eb
Assets/Tests/Board/AttemptScrambleTests.cs | level variant invariance tests | sys: Board | api: - | dep: BoardSession,AttemptScramble,BoardRulesTests,LevelData,BlockColourId | used: - | crit: K3 | note: colour set, grouping and column shapes survive 32 variants; five variants are pairwise distinct and each is stable; a cover key travels with the blocks that open it; the attempt RNG stays untouched | h:a390c4b2
Assets/Tests/Board/BoardUndoTests.cs | undo exactness and rng rewind tests | sys: Board | api: - | dep: BoardSession,BoardMove,BoardMoveHistory,DeterministicRandom,BoardRulesTests | used: - | crit: K3 | note: proves undo across reveal, completion, cover open and ice thaw by fingerprint comparison Also pins that undoing the move which finished a colour returns the exact fingerprint and makes the column liftable again — the lock is state, not memory. | h:d3bbac62
Assets/Tests/Board/ColumnModifierTests.cs | ice cover mystery and validation tests | sys: Board | api: - | dep: BoardSession,ColumnModifiers,BoardRules,BoardState,LevelData,BlockColourId,BoardRulesTests | used: - | crit: K3 | note: covers the D-009 trigger both ways plus every shape the data contract refuses | h:158a682a
Assets/Tests/Core/SaveServiceTests.cs | save file behaviour tests | sys: Core | api: - | dep: SaveService,SaveData | used: - | crit: K3 | note: drives the real file in a temp folder — versioned write, atomic replace, no economy defaults, all three settings toggles on in a fresh save, and every unreadable file kept aside rather than rewritten | h:e49bf490
Assets/Tests/Core/SaveMigrationTests.cs | save version gate tests | sys: Core | api: - | dep: SaveMigration,SaveData | used: - | crit: K3 | note: proves what is upgraded (a version 1 file gains musicOn on and reaches CurrentVersion, owing a flush), what is repaired (null lists, missing player id) and what is refused (unversioned, newer build); progression numbers are asserted untouched, and every version is asserted against SaveData.CurrentVersion rather than a literal so a bump does not have to be chased through the fixture | h:30b728eb
Assets/Tests/Core/AttemptSeedSourceTests.cs | attempt seed stability tests | sys: Core | api: - | dep: AttemptSeedSource | used: - | crit: K3 | note: four golden seeds pin the contract across builds, so changing the mixer breaks the fixture on purpose; 8x8 level-attempt pairs are distinct | h:0cc62a90
Assets/Tests/Core/ColorfulSort.Core.Tests.asmdef | core edit-mode test assembly | sys: Core | api: asmdef ColorfulSort.Core.Tests | dep: ColorfulSort.Core,UnityEngine.TestRunner,UnityEditor.TestRunner | used: - | crit: K3 | note: Editor-only and UNITY_INCLUDE_TESTS constrained; it can see Core only because Core has an asmdef of its own | h:1b65bdb7
Assets/Tests/Board/ColorfulSort.Board.Tests.asmdef | edit-mode test assembly definition | sys: Board | api: asmdef ColorfulSort.Board.Tests | dep: ColorfulSort.Board,UnityEngine.TestRunner,UnityEditor.TestRunner | used: - | crit: K3 | note: Editor-only and UNITY_INCLUDE_TESTS constrained, so it never reaches a build | h:ce1eb505
Assets/Tests/BoardView/BoardLayoutTests.cs | column grid, framing and hit-test tests | sys: BoardView | api: - | dep: BoardLayout,ColumnMetrics | used: - | crit: K2 | note: pinned to the art pack's own numbers and to Level 79's 12x4 shape; covers ragged boards — three over four, a hole inside a row, an empty row — and the property that a tap is the exact inverse of the layout on every one of them (D-033, D-034) Adds the HUD-band framing (the board clears both reserved bands, a width-bound board is untouched, and bands that leave no height throw) and the placement function, including the exact reported defect: an added column undone must return the authored cells and the authored row count. The metrics tests now pin both arts side by side under the single border rule — the pack's re-stated borders must reproduce the skirts they already had, and the hand-drawn tray must report zero cells in sprite so it stretches — plus the refusal when the borders leave no cell between them. Adds the tilted camera: the cosine framing, the refusal at 90°, a real orthographic camera whose every slot centre projects to the screen and reads back as itself at 0° and 25°, and the fixed-point check that three framings in a row leave the camera where it was. The tray's crown is pinned to the wave's own height and asserted to still rise above the studs it frames. Adds the row limit: the emptiest row wins, a tie goes to the row above so additions alternate, a row stops at the limit even where the grid has room, and a new row appears only when every row is full (D-070). The placement tests follow the turn rule now: the first added column takes the TOP row even when the bottom is emptier - the case that separates taking turns from choosing the emptiest - the rows alternate one at a time, and a full row is skipped without being owed a turn, which is why the top row can take two in a row (D-084). | h:d3a0e9c9
Assets/Tests/BoardView/BoardMoveAnimatorTests.cs | brick entry path tests | sys: BoardView | api: - | dep: BoardMoveAnimator | used: - | crit: K2 | note: the entry path only, which is the part that can be wrong invisibly: it starts on the brick and ends exactly on the cell, clamps a progress that arrives negative (a brick still waiting its turn in the stagger) or past 1, finishes its crossing before the drop begins and holds that x all the way down — the property that makes a brick enter through the column's mouth rather than its wall — never sags below either end when the target is a row lower, and still lands when handed a share the config would have refused (D-072) Rewritten for the three-leg path, and the shape assertions are the point: the fault they replace was invisible to every test the old path had, which checked both ends and was perfectly satisfied while the brick crossed diagonally in between. So they assert what must NOT happen in the middle - x still at the origin's until the apex is reached, x already the target's for the whole descent, and at most two steps moving sideways and vertically at once, which is 'no diagonals' counted rather than a sampled corner pretended sharp (D-081). Plus the completion contract: a motion completes exactly once and FinishNow cannot re-run one that already ran — the failure that would seat a run twice. It covers the unauthored-config path only, and says so: reaching the coroutine needs an authored config on a serialised object reference, settable only by reflection or by a production seam no feature has asked for (D-098). | h:33f8b866
Assets/Tests/BoardView/ColorfulSort.BoardView.Tests.asmdef | board view edit-mode test assembly | sys: BoardView | api: asmdef ColorfulSort.BoardView.Tests | dep: ColorfulSort.BoardView,UnityEngine.TestRunner,UnityEditor.TestRunner | used: - | crit: K3 | note: Editor-only and UNITY_INCLUDE_TESTS constrained; exists only because BoardView has an asmdef of its own | h:1a8953ab
Assets/Tests/Board/BoardSolverTests.cs | solvability search verdict tests | sys: Board | api: - | dep: BoardSolver,BoardSession,BoardMoveHistory,LevelData,AttemptScramble,TestBoards | used: - | crit: K2 | note: the three verdicts pinned separately, the returned solution replayed through the real rules to prove it wins, a locked ice column checked against the same board with the lock removed, and MaxDepth pinned to the undo history's cap so the two can never drift apart | h:3c77c4d2
Assets/Tests/Meta/ProgressionTests.cs | progression rule tests | sys: Meta | api: - | dep: Progression,SaveData | used: - | crit: K3 | note: the first tests over code whose mistakes are written to a player's disk, where a board can be undone and a save cannot — pins that a first attempt is attempt 0, that reading the attempt ordinal consumes nothing (a level that refuses to open must not eat a play), that a win raised twice advances once, that the last level clears without advancing, and that an ordinal outliving its database is dragged back into range The 'completing the same level twice advances once' test asserted the behaviour that let a save get stuck, and now asserts the opposite: replaying a cleared level and winning it still moves on. What that test was really protecting - a win not counted twice - is now pinned on the RECORD instead, which is where it actually lives (D-088). | h:40845589
Assets/Tests/Meta/ColorfulSort.Meta.Tests.asmdef | meta edit-mode test assembly | sys: Meta | api: asmdef ColorfulSort.Meta.Tests | dep: ColorfulSort.Meta,ColorfulSort.Core,UnityEngine.TestRunner,UnityEditor.TestRunner | used: - | crit: K3 | note: Editor-only and UNITY_INCLUDE_TESTS-constrained like the other three; it can reference Meta at all only because Meta got its own assembly in 4A (D-036) | h:6983e853
Assets/Tests/Board/BoardBoosterTests.cs | add-column and shuffle rule tests | sys: Board | api: - | dep: BoardSession,BoardMove,BoardState,BoardColumn,LevelData,TestBoards | used: - | crit: K3 | note: both boosters are new shapes for BoardMove, so every case ends on TestBoards.Fingerprint — cells, hidden flags, locks, completed colours, history depth and RNG cursor — compared against the string the attempt started with, because a shuffle whose undo is off restores a board that looks right and nothing on screen would say so; covers the ceiling refusal, the deadlock rescue, add-then-fill undone in order, hidden cells and locked columns left alone, and five shuffles undone back to the start | h:b5e561a1
Assets/Tests/BoardView/BlockNeonTests.cs | brick-colour-as-light tests | sys: BoardView | api: - | dep: BlockView | used: - | crit: K2 | note: pins the formula that lights both the lifted run's glow and a flying brick's trail — the hue's ratios survive, the strongest channel goes to full, a lift raises the weak channels without closing the gap that makes it a colour, full lift is white, and a brick with no hue (the near-black ? and the placeholder white) is left alone rather than divided by nothing. It is tested because it was arrived at the hard way and because one change here changes two effects (D-063, D-073) | h:fb0845fb
Assets/Tests/BoardView/BoardAnimationConfigTests.cs | burst config range tests | sys: BoardView | api: - | dep: BoardAnimationConfig | used: - | crit: K3 | note: both bursts' numbers only, because they are the ones that arrived after the asset did: the fixture is a config authored before either existed, so it carries 0 for all of them, and the test that matters says 0 is legal - refusing it would report the whole animation asset invalid over a number nobody set, which is a board that snaps instead of moving. The two conditional rules are the other half, and they are the ones that fail SILENTLY: the landing's speed is height divided by seconds, so sparks with no duration is a division by zero, and a finish spark with no lifetime is emitted and gone in the frame it was made - both look exactly like a burst that never fired. The shipped values are pinned too, so the feel and the validator cannot drift apart unnoticed. JsonUtility over the serialised fields is what lets a test say "this asset, but with one number changed" without a second copy of every field (D-077, D-078). The glow fade joins them, refused when negative because a backwards lerp would leave the symbols brighter than they started on a column about to darken (D-079). The two spark sizes join them, refused when negative; the shipped fixture tracks the retuned landingRiseSeconds (D-081). The four new numbers join them, and the dip's upper bound is the one worth having: it is the only field here whose bad value is neither negative nor zero but simply too large (D-082). The two scatters join them (D-083). The shipped revealTurnDuration is pinned in AsShipped, and one test states the rule the rest of the file follows: an unauthored turn is 0, 0 is legal, and 0 means no turn (D-099). The pinned field is revealFadeDuration now, renamed with D-101 — and the test says so, because a rename is exactly the edit that drops a serialised value unnoticed. | h:aba34991
Assets/Tests/Content/ColorfulSort.Content.Tests.asmdef | content edit-mode test assembly | sys: Content | api: asmdef ColorfulSort.Content.Tests | dep: ColorfulSort.Content,UnityEngine.TestRunner,UnityEditor.TestRunner | used: - | crit: K3 | note: Content's first test assembly, Editor-only and UNITY_INCLUDE_TESTS-constrained like the other four; it exists because the symbol extraction was deliberately kept out of Assets/Editor/ so it could be reached at all (D-035, D-080) | h:23f0d16b
Assets/Tests/Content/SymbolMeshBuilderTests.cs | symbol extraction geometry tests | sys: Content | api: - | dep: SymbolMeshBuilder | used: - | crit: K3 | note: index arithmetic over two submeshes, which is the shape of code that is wrong invisibly - a list remapped one index out still produces a mesh, just the wrong one, and on screen that is a shrug rather than an error. Pins that only submesh 1 comes across, that every index lands inside the new mesh's own vertices, that it is centred and one unit wide, and that the half turn happened. The fixture's symbol is deliberately ASYMMETRIC: recentring cancels a translation, so a symmetric shape could not witness the rotation at all and the test would pass either way (D-080) | h:aef78e83
Assets/Tests/Content/LevelCodecTests.cs | level file format and conversion tests | sys: Content | api: - | dep: LevelCodec,LevelDefinition,ColumnDefinition | used: - | crit: K2 | note: two kinds, and the second is the point. Round-trips pin the encoder and decoder against each other, which catches a format that cannot express a hidden cell or a thaw count but says nothing about whether the file in the repo holds the level it should - and THAT failure is silent, because a level decoded into a different board plays perfectly well. So the last test opens the shipped Levels.json off disk and checks it against the board Level_0000.asset had. Kinds are asserted through the encoded string rather than through ColumnKind, which lives in Board and is not referenced here; it turned out the better assertion anyway, since one string pins the kind, the capacity and every cell at once and is the form the file actually holds (D-085). The shipped-file test now asserts the file is readable and EMPTY - the converted level was deleted so levels could be authored from 1 - and a second test pins that a level numbered 0 is refused while 1 is accepted (D-086). Two corrections. The shipped-file test asserted the file was EMPTY, which was true for about an hour and would have failed the moment the first level was saved - a test that breaks when the project starts working is worse than none - so it now asserts what lasts: the file reads and every level in it validates and is numbered from 1. And the numbering fixture was n2:aa, a capacity-2 column holding two of one colour, which is a colour already gathered and refused outright (D-013), so it failed for a reason that had nothing to do with numbering (D-087). A MinimalLevel helper builds a level that satisfies EVERY rule Validate runs, because this file's numbering fixture broke three of them in three attempts - a colour already gathered, then the two-column minimum - and each failure reported something the test was not testing. It is offered and not imposed: the other tests build their own levels on purpose, since a round-trip has to work on a level Validate would reject as much as on one it accepts (D-089). | h:e4b5aaf8
Assets/Tests/Meta/PlayerEconomyTests.cs | the purse's rules, pinned | sys: Meta | api: 10 tests | dep: PlayerEconomy,EconomySeed,BoosterId,BoosterIds,SaveData | used: - | crit: K2 | note: the economy writes to a player's disk and a save has no undo, so what is pinned here is the silent-and-permanent set: seeding that runs twice (AttemptStarter rebuilds the economy per call, so a re-seed would pay per press), seeding that never runs, zero charges being mistaken for a never-seeded save, a half-completed purchase, and two boosters sharing one save id. Plain C# with a SaveData fixture and a counting delegate - no ScriptableObject, no filesystem - which is the split EconomySeed exists for | h:8c9d9861
Assets/Tests/Core/DisplayConfigTests.cs | frame-rate ceiling contract | sys: Core | api: - | dep: DisplayConfig | used: - | crit: K3 | note: the two values easy to get wrong in opposite directions - 0 must stay legal (an asset written before the field carries it, and refusing it would invalidate a config over a number nobody set) and a negative must not. Zero is the interesting one and is pinned twice: it validates AND it reports PlatformDefault, because "legal" and "neutral" are different claims and on mobile the platform rate is 30 (D-100). Authored fields are driven through JsonUtility.FromJsonOverwrite, the same seam BoardAnimationConfigTests uses to reach private [SerializeField]s. | h:48e5cba2
Assets/Tests/BoardView/ColumnRevealTests.cs | reveal colour blend contract | sys: BoardView | api: - | dep: ColumnView,RevealLook,BoardAnimationConfig | used: - | crit: K2 | note: the property the whole reveal rests on, pinned: at the MIDPOINT the symbol is exactly the body colour. The symbol is embossed geometry so the shape changes with the skin whatever the colours do; painting it its body colour is the only thing making that change unreadable, and the skin is swapped at precisely that moment. Also pinned: continuity across the midpoint, because the function is written in two branches and two branches are where a seam appears - disagreeing there flickers the brick on the exact frame the skin changes, which is the pop this whole change removed. Fixture colours are far apart on every channel so an assertion cannot pass by two colours happening to be close (D-101). | h:f6c83aa0

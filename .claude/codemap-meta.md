<!-- stamp: a082d5f 2026-08-21 status: OK -->
Assets/Scripts/Meta/ColorfulSort.Meta.asmdef | progression assembly definition | sys: Meta | api: asmdef ColorfulSort.Meta | dep: ColorfulSort.Board,ColorfulSort.Content,ColorfulSort.Core,ColorfulSort.BoardView | used: ColorfulSort.UI | crit: K1 | note: added in task 4A because an asmdef assembly cannot reference Assembly-CSharp, so UI could not see AttemptStarter without one (D-036); makes the blueprint's Meta arrows compiler-enforced the way D-016 and D-024 did for Core and BoardView; autoReferenced, so Assembly-CSharp-Editor and BoardViewPrefabFactory still compile against it | h:8064ad4b
Assets/Scripts/Meta/AttemptStarter.cs | opens the current level, ends it, and carries the boosters | sys: Meta | api: AttemptOpened; AttemptWon; AttemptDeadlocked; BoardChanged; EconomyChanged; OpenedLevel; CanUndo; CanAddColumn; CanShuffle; Coins; LastWinAward; ChargesOf(BoosterId); OfferFor(BoosterId); CanBuy(BoosterId); TryBuyBooster(BoosterId); StartAttempt(); Restart(); PlayNext(); Undo(); AddColumn(); Shuffle() | dep: Progression,ProgressionConfig,PlayerEconomy,BoosterId,BoosterOffer,LevelDatabase,LevelDefinition,BoardSession,BoardMove,AttemptScramble,AttemptSeedSource,GameRoot,BoardView | used: GameplayHud,PausePopup,WinPopup,FailPopup,BoosterButton,BoardViewPrefabFactory | crit: K2 | note: the attempt seam (D-027) — level, variant and attempt all come from progression, and the seed is built from the ORDINAL (D-039); the play is recorded only after the board is up (D-040); a win marks cleared and advances here, never in the popup. It is also where the three boosters live as commands: they cost nothing (D-042, D-043), each redraws the board through BoardView.Resync, and BoardChanged is raised from the session's own MoveApplied/MoveUndone so the bar re-reads what is available without polling Progression is built fresh on every call instead of cached: it takes the level COUNT as a number, so an instance made when the database held one level went on believing there was one after three more were authored, and HasNext stayed false. It owns nothing of its own, so per-call costs an allocation and buys a count that is always real. And the win is DEFERRED: BoardSession.Won fires the instant a move is legal, with the winning bricks still in the air, so it is remembered and announced on BoardView.BoardShown - D-076's split applied to the win itself (D-088). The boosters cost a charge again (D-091 reopens what D-043 closed) and the ORDER inside Spend is the whole rule: charge checked first so a player with none cannot use one, mutation applied second, charge taken LAST so a booster the rules refused is free. A first clear pays coinsPerLevelCleared and a replay pays nothing - IsCleared is asked BEFORE CompleteCurrentLevel marks it, because the last level can be replayed for ever and a paid replay would be a coin tap. LastWinAward is what the win screen flies across; zero means there is nothing to fly. PlayerEconomy is rebuilt per call beside Progression and for D-088's reason - it caches nothing, so a stale one could only ever be a way for the save and the screen to disagree - which also means building it is what SEEDS a first-time player, so nothing has to remember to call an Initialise. | h:98faa326
Assets/Scripts/Meta/Progression.cs | where the player is, and the save's writer for it | sys: Meta | api: Progression(SaveData,int,Action); LevelCount; CurrentOrdinal; HasNext; AttemptOrdinal; PlaysOf(int); IsCleared(int); RecordAttemptStarted(); CompleteCurrentLevel() | dep: SaveData,LevelProgressRecord | used: AttemptStarter | crit: K1 | note: the single writer of currentLevelOrdinal, cleared and plays — SaveData's own doc already assigned them to Meta, and Core still owns the bytes; plain C# taking a level COUNT rather than the database, because progression is about ordinals and the database is about which asset an ordinal names, which is what makes every rule testable without a ScriptableObject fixture; reading and writing are split on purpose (AttemptOrdinal costs nothing, RecordAttemptStarted is called only once a board is up); CompleteCurrentLevel is idempotent so a win raised twice cannot advance twice; a constructor Repair drags an out-of-range ordinal back, because a save outlives the database that produced it CompleteCurrentLevel no longer returns early on an already-cleared level. That early return conflated 'I have finished this before' with 'there is nowhere to go' - two questions answered by the record and by HasNext - and it is what let a save get STUCK: a first win while the database reported one level marked the record and could not advance, and every win after it hit the return. The save heals on the next win with no migration (D-088). | h:455c64b2
Assets/Scripts/Meta/ProgressionConfig.cs | progression's tuning numbers | sys: Meta | api: VariantCount; StartingCoins; StartingBoosterCharges; CoinsPerLevelCleared; Seed; OfferFor(BoosterId); BoosterOffer.Booster,Charges,Price; Validate(out string) | dep: BoosterId,EconomySeed | used: AttemptStarter,BoardViewPrefabFactory | crit: K2 | note: type with the system that reads it, asset in Data/Config/ (D-023); holds the variant count because Board knows how to build a variant but deliberately knows neither the count nor which to play (D-015); no field default, so a fresh asset is invalid and says so; 5B's starting hearts, coins and booster charges land here The economy's numbers landed here as 5B always planned: starting coins, starting charges, what a first clear pays and one BoosterOffer row per booster. Handed to PlayerEconomy as an EconomySeed STRUCT rather than as this asset, which is what keeps the code that writes to a player's disk testable with no ScriptableObject fixture. Validate refuses a missing offer row and a duplicate one - the duplicate is the worse of the two, since OfferFor returns the first and the second is a price nobody is ever charged (D-091). | h:66fe8af9
Assets/Scripts/Meta/BoosterId.cs | which booster, and its save key | sys: Meta | api: BoosterId.AddColumn=0,Undo=1,Shuffle=2; BoosterIds.All; BoosterIds.SaveIdOf(BoosterId) | dep: - | used: PlayerEconomy,ProgressionConfig,AttemptStarter,BoosterButton,BoosterShopPopup,UiStyleConfig,GameplayHud | crit: K2 | note: in Meta and not UI because what a booster COSTS is progression policy and UI only says which one it means (D-091); numbered explicitly and only appended to, since the three BoosterButton instances carry these ints and an inserted value would turn Undo into Shuffle - the same trap Setting was numbered against (D-045); SaveIdOf is a hand-written string rather than ToString(), because SaveData keys charges by string on purpose and a renamed enum member must not reset a player's charges to zero | h:b4c0b80d
Assets/Scripts/Meta/PlayerEconomy.cs | the purse and the booster charges | sys: Meta | api: EconomySeed(int,int); EconomySeed.Coins,ChargesPerBooster; PlayerEconomy(SaveData,EconomySeed,Action); Coins; ChargesOf(BoosterId); CanAfford(int); TrySpendCharge(BoosterId); Award(int); TryBuy(BoosterId,int,int) | dep: SaveData,BoosterId | used: AttemptStarter | crit: K1 | note: the single writer of save.coins and save.boosters, the shape Progression already has for the level fields - it changes the save in memory and says so, Core still owns the bytes (D-091). ONE class for coins and charges rather than a Wallet plus an Inventory: a purchase moves both in one breath and the seeding rule binds them, so splitting them would give a buy two writers to keep in step. Seeding needs NO saveVersion bump - coins and boosters were already in the shape, left there when D-043 cancelled the economy - and the marker for "never seeded" is an EMPTY booster list, which tells a fresh save apart from a player who has spent every charge (three records reading zero). Takes an EconomySeed struct rather than the config asset for the reason Progression takes a level count: it keeps the code that writes to a player's disk testable with no ScriptableObject fixture. Repair fixes structure only - a null list, a record with no id, a negative balance - never policy, so an honestly empty purse stays empty | h:68c1a3df
